# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.3.r0.geab0ea0
pkgrel=5
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
a3deac00f679804122796cddf2529c2d51b2700d # scx_loader: Add cidland scheduler
dc2b63e2292f80349def53f182ced2a887c8ba86 # scx_loader: Add chaos scheduler
1b473daead759c236eeec662365f6127a3326ab2 # scx_loader: Add maestro scheduler
91cae686ce1c80c784081371ff1569cc00fb8c86 # scx_loader: consolidate scheduler state into SchedState
818d84369b7eaa883ed824847951fc7aa654e44e # scx_loader: emit one atomic PropertiesChanged per transition
1d06188f3fe5152a2cf7cc9a25bd209312f00c78 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
03d84311bcbc5c365694922f206d256f66f6c0d3 # scx_loader: document the property change-signal contract
1d75735aa6524a78403951666759ee024512d43b # scx_loader: announce the instance when the daemon starts
d34a15ea641164cf629ba47ec90a085af52e1be4 # scxtui: follow scheduler state live via PropertiesChanged
d0ba7ea4ec5f10d24cc2fae625adf4dfb3f0fbf9 # scxtui: re-ask the daemon when the kernel disputes its story
05f75dcb987ae87bfff18e75bd7e38d16deb9db7 # scx_loader: log the selected configuration source
b8d8d1bb45fef07501710b4d5b64e04258653057 # scx_loader: add power-profile configuration model
a5a63f5ce6617dfae04cb2e0dfbd41793da2e02c # scx_loader: add pure power-profile resolution and apply-decision policy
704e3558a9535b7fc93fe7b65319458bcdac4a85 # scx_loader: monitor power-profiles-daemon and apply mapped modes
566eb24e9a6e485e4a5007ac861c4ee063827199 # scx_loader: cap PPD retry backoff at the full 30s duration
484a1b6e959c7fd711563831005d668f3f2b478a # scx_loader: clarify PPD monitor handle intent and reconnect logging
2a8e127362dd91430f4c5d0aac87406d7af77975 # Document power-profiles-daemon integration
29420ada73fd83b3efc8557931b281750fc532ce # scx_loader: enforce the PPD mapping at the moment a scheduler appears
31db5afb180316fe870970412d583f5a939334a9 # scxctl: report a coherent loader snapshot after start and switch
936d9eec4fe4c40152bfd4efdc5c023f387231a9 # scxctl: drop the causal success path from post-operation reporting
57340b7748c79a69b7428f876e9ebde42be87b8e # scxctl: restore the unconfigured-mode truth in the observation line
b5ad6a3250bcc8d0924aed97516e0b59aa1c5221 # scxtui: annotate an unconfigured running mode in the status panel
f29bcf4da23de56b3cf5ce271c1c7fb6d21a1dbc # scx_loader: watch the child task to completion in the runner
84b5c15cde987f5b2e16de7cd5693a3efa8a5826 # scx_loader: give every scheduler spawn an identity
c272a891ca9c8f424c0376592c6af1f419fb4fc8 # scx_loader: reset state and signal when the scheduler is gone for good
d4d5bd79ed5bc9d6585014a626b33d2a784fc909 # scx_loader: back off between respawn attempts and renew the budget
f0bd36351ff2b122d83081ec0bfb1c9df7ec7028 # scx_loader: document the exit-path contract
9be3ccc8d1d0e6f296f8e7ea02c806e150656e37 # scx_loader: refuse StartScheduler while a scheduler is running
f4ee3e5b59415beb28e574054e7931e98e0953dc # scx_loader: group the mutable scheduler state under one owner
255817e6acaee545888bd4351c862308a8fafd30 # scx_loader: move the mutable state behind its own lock
9f99acd848e372e3f0dc013051c1d59833fb4167 # scx_loader: authorize before taking the state lock
4986ea66583897bfb6cb5a26c70bb8fcd76d7959 # scxctl/scxtui: read snapshots until two consecutive answers agree
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
