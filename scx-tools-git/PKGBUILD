# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.2.r5.gd7acb09
pkgrel=4
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
30fc2ca780ffbd397a7bed62eb8d9fee7141eeb3 # scx_loader: Add chaos scheduler
ed8481db25424ecedf1de9a72ddcc331a6b7b822 # scx_loader: Add mitosis scheduler
9690c50a8f27a1310ad2c4b692f7acc5fe4b9b7c # scx_loader: Add maestro scheduler
00c08970b69dc1432888cb92d331fdbe4f6628c8 # scx_cake: update profiles flags
72e5989277058a5777be91470113addcf432819f # scxctl: send error messages to stderr
b5d26302c5fafb5ec13cb6888a39c2ad7581b2b7 # scxctl: propagate D-Bus errors from cmd_list
e093a44ea761ee7c3d4b13a6446b4a6709ab3c65 # scxctl: stop unwrapping in validate_sched, resolve names via a pure helper
dd52be43640b022d8ca00145a1039c63383e6efd # scxtui: add D-Bus scheduler management TUI
a59e2d963744ddc671730d4d568bd016a19b4605 # scxtui: add journal log viewer
f2ec06416dc01ad5b31903c58c50f3c6c8c80174 # scxtui: add scxtop monitor integration
6f3b03f0e4188b453e91fce17a568f90f1447e19 # scxtui: report kernel sched_ext state
b5765726895ae0b2ecbe416d8b67e9c9d9628e9b # scxtui: support the scx.service backend
7d8b1ececfdbbd41ebb5556f5abf016029f3fb4c # scxtui: update README.md and bump version
f28c9a1fc62f3f59604ebf3b28e53cd53bdb10d6 # scxtui: make clippy happy
39c1cc868ce364a7bae403418fc7b923a0e284cd # scxtui: address first review feedback
911237541ab594e734af8c6687827f9ad89cb7ca # scxtui: make clippy happy
27cd6d66b260793019af0f7c208eafc0ab96fb5a # scxtui: fix broken README.md
e95c1f74ac54f78fb51452153819cd6d0acb42bc # scxtui: clarify backend switch failure message
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
