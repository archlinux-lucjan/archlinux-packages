# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.3.r1.g30e64eb
pkgrel=2
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
ac5f07dda79a82106e545b692ffa565989e16ca7 # scx_loader: Add eevdf scheduler
d5dde2c866b5b6607ac321cdffccaa8b95fbf49b # scx_loader: Add chaos scheduler
a23c2f944ae2f3464f45201bd0d7517ce8e72d92 # scx_loader: Add mavd scheduler
1d20ae87d1a09a4401ca5ac33897f3e485f2f0b2 # scx_loader: Add maestro scheduler
144574b59e440e48d2fb8362b5f547959f1d61d4 # scx_loader: consolidate scheduler state into SchedState
4401085fa0eef78b0804fb0591f9255ec984f132 # scx_loader: emit one atomic PropertiesChanged per transition
3ebee07f0b2602a10345bbd89b94664a8b66cb20 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
03559e342c95b88a7b550c201c24cc48393a9b25 # scx_loader: document the property change-signal contract
aac7b13c889291e632e250f4f9ba7933feeb4672 # scx_loader: announce the instance when the daemon starts
ca4ead61c5d97fe65f794b005081c5470d382513 # scxtui: follow scheduler state live via PropertiesChanged
57e4d0e7cc6f451b5d4cb284f4565a6f742ecd67 # scxtui: re-ask the daemon when the kernel disputes its story
33b1722479f18fd5070dbbb524d6362f9ee0a119 # scxtui: pin that a pushed movement feeds the annotation
63d6bc75bf2b13d9dd3439bba0928a89396cd4b9 # scx_loader: add power-profile configuration model
296dd32e04aeb06eb2f8a7b65be0f3ec4c9b7fc1 # scx_loader: add pure power-profile resolution and apply-decision policy
f24b75791181e19427793c5862d9286293704d6b # scx_loader: monitor power-profiles-daemon and apply mapped modes
e3305e542105c65d473f09e3ef72ebeebcf2a2bb # scx_loader: cap PPD retry backoff at the full 30s duration
7de564399fda34923c84bfca4420ceda78327c94 # scx_loader: clarify PPD monitor handle intent and reconnect logging
bff5bffd9a53466763181eabf92477dd5193098a # Document power-profiles-daemon integration
1161606e0a46778a87e67e5dc24f82aaacc6d126 # scx_loader: enforce the PPD mapping at the moment a scheduler appears
454b237433b4f57440db33c25cbdf002d4ecb163 # scxctl: report a coherent loader snapshot after start and switch
ffcfc4e45bbdaa6ac800ced2f3d536db66384bd5 # scxctl: drop the causal success path from post-operation reporting
60d0a4a3424184b9e0ab1f3f5d428790d972ac45 # scxctl: restore the unconfigured-mode truth in the observation line
4bb5bd892cda6e0de1ca7a1ec5fbf8acbf0cce65 # scxtui: align the status annotation with scxctl's observation wording
19723e7ffed66e1a1ecd25c4f91b60d8895c3a08 # scx_loader: watch the child task to completion in the runner
1daf6a15ecdb441eabfb2a65fbedeff696ec56d0 # scx_loader: give every scheduler spawn an identity
f66e9b6c681b86a9a38059ccc4de78a86858c109 # scx_loader: reset state and signal when the scheduler is gone for good
d6f9089d8ba7d29b4c1041f492cfddeca35d43f6 # scx_loader: back off between respawn attempts and renew the budget
1bafb5ecf876b81fc9d0158fe94094f7e7ea2c9e # scx_loader: document the exit-path contract
0f30145c2d76c617a4fee780eaeb44636ab13fd4 # scx_loader: refuse StartScheduler while a scheduler is running
30bb271fbb9a6329a67ebb65633be818e7d383cc # scx_loader: group the mutable scheduler state under one owner
6fc4ceefcc1ee874bdf253745b6cf9999a38f0a2 # scx_loader: move the mutable state behind its own lock
99f571f7cb94a39c0bf839bdd5ec71f20fb67d3e # scx_loader: authorize before taking the state lock
293642d52f4aa72add3d1235f5b16b937779e019 # scxctl/scxtui: read snapshots until two consecutive answers agree
835ef53f821e8936d088973128a76080813b16ce # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
