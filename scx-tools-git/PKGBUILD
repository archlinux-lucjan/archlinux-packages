# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.2.r8.gcda5deb
pkgrel=16
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
eab5b64bd0f95e016126789e7088346d523f4dce # scx_loader: Add chaos scheduler
b52acf8a001bc4fa50570970a06173d549aa9b2f # scx_loader: Add mitosis scheduler
0f60a65fcfaa549d00cf726517f7f9d6cd9b7dc1 # scx_loader: Add maestro scheduler
ca06e3d8bd544c47db9b86660dd1c9352c88c314 # scx_cake: update profiles flags
32393c9af385ff50ac4c97c182595fdec6391a66 # scxctl: parse scheduler arguments as shell words
7be3fe4930396923c1cb38cb2b2bdf7290c54a23 # scxctl: support shell-style scheduler arguments
9ed8b0cd11296e9c6d509d2b29e26fb285b729fd # scxctl: correct overclaims in argument-handling documentation
fed2769cbcb57a49279bf38664a85505d0945b26 # scxctl: make clippy happy
8f6804d4cefe5a440e3f3fe3a856d920913aa512 # scxtui: query per-mode arguments instead of the configured-mode list
790ceb2f7b41cd78d9c15f520250eb8f66fb26a1 # scxtui: cache mode arguments, invalidate on daemon restart
0589ba96120f9269d55c60d0a81e88937c6cfe21 # scxtui: preview configured mode arguments in the status panel
0fb90c75cb50882166207b4fc2134a9c383fc8a0 # scxtui: render running scheduler arguments as shell words
5daa71a20939a3ec94bf2a1942296cd63cdead79 # scxtui: document the mode-argument preview
3555fc46890cb28aca12d6494017e4820f35c1f2 # scxtui: add a session-only custom-arguments field
5e16e3a8e07b454b0dc5fd6266bffb4b839e1b34 # scx_loader: consolidate scheduler state into SchedState
7e84b0e94d251458cb47f06f6deb7d5130355351 # scx_loader: emit PropertiesChanged when scheduler state changes
81972c2a9523e01f5b82fc5f068b7e90175130b0 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
ac640c492ecb4b95944ee56c0865b5f479f98ef9 # scx_loader: document the property change-signal contract
1d419186ec7f2d2dd38fb28c411048453be36447 # scx_loader: compare CurrentSchedulerArgs by its observable value
ceb7815e4d73a616b816c261289a80bebfaf3ccc # scx_loader: emit one atomic PropertiesChanged per transition
243dbfeaf16884a30826f0a19bbbbfa940b6a003 # scx_loader: declare const properties in the runtime introspection
c282160320ebab554eb25dbb0b2cd245aaed133e # scx_loader: announce the full state when the daemon starts
24b98bdfdb50a44887889858d25de202498371db # scx_loader: announce the live state under the interface lock
6932562e9a722b5db464f139e88bf4c33b9e3523 # scx_loader: expose a DaemonGeneration for value-blind replacements
98184fcf5e098dd8db060f4462529714cca2a227 # scx_loader: derive the generation from the connection's unique name
a08972b1802d779892daf08017be1dde4a57b370 # scxtui: follow scheduler state live via PropertiesChanged
51aa79f0af0caaa795aaddec54d580f444cb3e89 # scxtui: re-ask the daemon when the kernel disputes its story
552a06c90a284c3282bbb730c6760ad2697503bd # scxtui: carry only runtime state on the push channel
1972db252836c2721d8a7433fc1ded8db302f0e4 # scxtui: recompute the dispute edge from post-refresh truth
2bb7d6e90c171bcf49e54a295369b153f6d2a86b # scxtui: never apply a snapshot read across an owner change
8910e89481691b4fca8f1b869dfe253c6b17a760 # scxtui: treat the push cache as a doorbell, never as a payload
7604cadae14fd4915751cc7953cda38071356e9d # scxtui: require a successful fetch before trusting the push
a2eaecdded768b617fe62a7e2f74a7e3166611f7 # scxtui: hear value-blind replacements through the generation
0689e11dcca9a69c21520c593d1f7665673d4dd3 # scxtui: carry the generation as the daemon's unique bus name
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
