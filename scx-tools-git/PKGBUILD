# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.3.r2.g247fd05
pkgrel=1
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
9129803097f54a04506b17f331ab30844317ca76 # scx_loader: Add chaos scheduler
e188cbead77f7fa8bb0a0259f91c12d4b9f83554 # scx_loader: Add mavd scheduler
c0bc2d1d211a9705261b0db4f18fbcf3c7ffda9e # scx_loader: Add maestro scheduler
bd60dad6a294ffe8bea94f96e7c86bdaf1f1bc0c # scx_loader: consolidate scheduler state into SchedState
9679514613a7d5fa81d15b7ed5e2ca3f1e970356 # scx_loader: emit one atomic PropertiesChanged per transition
11bfe46224cc7172d0175050bfcdb12e74e132d2 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
56aa8050908365513abfee7d8764dad2ceeca120 # scx_loader: document the property change-signal contract
749d1e5aff759095300dcd1cb423ff968f4fc906 # scx_loader: announce the instance when the daemon starts
485ef65f2d3f3ee0f68802867dd7af18ee8b5e89 # scxtui: follow scheduler state live via PropertiesChanged
ff5639a5a67ee1386a27abc6071534651d92bd01 # scxtui: re-ask the daemon when the kernel disputes its story
2182470e5b36f377d3ddd5851478ac7f38588c38 # scxtui: pin that a pushed movement feeds the annotation
3b19b00af4a1882d4d9d1f92a0c3e6fc0a323f3e # scx_loader: add power-profile configuration model
e0f6fe362c4131fa3259ffb6408ef7f24789ba34 # scx_loader: add pure power-profile resolution and apply-decision policy
c61d6194224694a87eb32a1897f3bc87f97afef6 # scx_loader: monitor power-profiles-daemon and apply mapped modes
324a4b53fbaac5b243892a0eec8bba2ae61483b1 # scx_loader: cap PPD retry backoff at the full 30s duration
ca5ad83688bd546c3ed03d656e7c1113858e1774 # scx_loader: clarify PPD monitor handle intent and reconnect logging
e856681b043313f55790aff2716dd5734211c07c # Document power-profiles-daemon integration
6afd8bc1d77c3d7ef3f638806af5bfba060a49da # scx_loader: enforce the PPD mapping at the moment a scheduler appears
ee864cb580a844aec0bccacdc4a5ff50c5ff450f # scxctl: report a coherent loader snapshot after start and switch
d50138f6254396e7a7eaa0042c65bc5f218ba488 # scxctl: drop the causal success path from post-operation reporting
8a7ab99e4be51680aaf5491e893208449f4202f1 # scxctl: restore the unconfigured-mode truth in the observation line
de193cc4519e7b6ca021731df856337a13416559 # scxtui: align the status annotation with scxctl's observation wording
ac906b780be8e5804afc3c4ed1fe259dc45911f1 # scx_loader: watch the child task to completion in the runner
b08431ed9be601a8d193052b5592f685de16a823 # scx_loader: give every scheduler spawn an identity
86413e94a315b2fa000e5b3b338b69e130301767 # scx_loader: reset state and signal when the scheduler is gone for good
1c9e0f1ee29d0400740f7dacc26ae136c31083f2 # scx_loader: back off between respawn attempts and renew the budget
1edde83e2d147a13368c582d504d5e1931dc149d # scx_loader: document the exit-path contract
180c969f55fe8264d2393f3519946397853d8039 # scx_loader: refuse StartScheduler while a scheduler is running
ca5a8519a186eecef1dd28b54a722823d534a1af # scx_loader: group the mutable scheduler state under one owner
66698544d6b917625f0b89396105b2d6cbe31b1c # scx_loader: move the mutable state behind its own lock
b1cd1dabdd27eeec946337d0a40e273c75b83c48 # scx_loader: authorize before taking the state lock
5f6381d32f2250dc15f2cb06224ea41a49164d2b # scxctl/scxtui: read snapshots until two consecutive answers agree
04d38ba1bbbaa4dace2a8a2b872856f179f70957 # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
