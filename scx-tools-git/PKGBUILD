# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.2.r8.gcda5deb
pkgrel=15
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
eab5b64bd0f95e016126789e7088346d523f4dce # scx_loader: Add chaos scheduler
b52acf8a001bc4fa50570970a06173d549aa9b2f # scx_loader: Add mitosis scheduler
0f60a65fcfaa549d00cf726517f7f9d6cd9b7dc1 # scx_loader: Add maestro scheduler
ca06e3d8bd544c47db9b86660dd1c9352c88c314 # scx_cake: update profiles flags
32393c9af385ff50ac4c97c182595fdec6391a66 # scxctl: parse scheduler arguments as shell words
7be3fe4930396923c1cb38cb2b2bdf7290c54a23 # scxctl: support shell-style scheduler arguments
9ed8b0cd11296e9c6d509d2b29e26fb285b729fd # scxctl: correct overclaims in argument-handling documentation
fed2769cbcb57a49279bf38664a85505d0945b26 # scxctl: make clippy happy
8f6804d4cefe5a440e3f3fe3a856d920913aa512 # scxtui: query per-mode arguments instead of the configured-mode list
790ceb2f7b41cd78d9c15f520250eb8f66fb26a1 # scxtui: cache mode arguments, invalidate on daemon restart
0589ba96120f9269d55c60d0a81e88937c6cfe21 # scxtui: preview configured mode arguments in the status panel
0fb90c75cb50882166207b4fc2134a9c383fc8a0 # scxtui: render running scheduler arguments as shell words
5daa71a20939a3ec94bf2a1942296cd63cdead79 # scxtui: document the mode-argument preview
3555fc46890cb28aca12d6494017e4820f35c1f2 # scxtui: add a session-only custom-arguments field
7709e9311dc0e3dff60414b689960f95e141921d # scx_loader: consolidate scheduler state into SchedState
675a173868f55593d3d5eb54acc49a665cde2c02 # scx_loader: emit PropertiesChanged when scheduler state changes
23c1dbed10b489c024ebf699b9bab6fe62841145 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
496719f6ad55fdf98d8190efe804b5f55a300c39 # scx_loader: document the property change-signal contract
ad07654087fcb508dbe119386215f5dfeee27312 # scx_loader: compare CurrentSchedulerArgs by its observable value
5ed68fcf72d579ce1a1c30c6f23e950b4aa9ba63 # scx_loader: emit one atomic PropertiesChanged per transition
ed1e43c236a4233dbcae6e91923b78c89ee1941c # scx_loader: declare const properties in the runtime introspection
727dfedd6edc12ce516c7677105cf107f43d0f6b # scx_loader: announce the full state when the daemon starts
bf7df5fea39dcc628bcac1ba19e603a8cf2fd30a # scx_loader: announce the live state under the interface lock
8fc1b14336616e18e5811bd3e99ffd27847fb2ab # scx_loader: expose a DaemonGeneration for value-blind replacements
b1dcfa48ababa7c505546079d00b4a6845281010 # scxtui: follow scheduler state live via PropertiesChanged
f99087ea10d546de03f04fa85e594a590e7c89e0 # scxtui: re-ask the daemon when the kernel disputes its story
d5b6ba397a73d1f92726fb156c6c6fc594742e7c # scxtui: carry only runtime state on the push channel
c6aa87042cb4b7b2df4c6e4186619c45d1af1e55 # scxtui: recompute the dispute edge from post-refresh truth
be68d83aaea1cf801424c762ef3e0279dce867ce # scxtui: never apply a snapshot read across an owner change
9a440db98622be5c07af1e262d5c8a1d4e8db216 # scxtui: treat the push cache as a doorbell, never as a payload
f6527b5259da73504374ec4ba8de29368ffd4651 # scxtui: require a successful fetch before trusting the push
dbc43b84213832b80a2476ff4aedc98d17c9c1db # scxtui: hear value-blind replacements through the generation
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
