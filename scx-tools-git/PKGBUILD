# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.3.r0.geab0ea0
pkgrel=8
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
a3deac00f679804122796cddf2529c2d51b2700d # scx_loader: Add cidland scheduler
dc2b63e2292f80349def53f182ced2a887c8ba86 # scx_loader: Add chaos scheduler
1b473daead759c236eeec662365f6127a3326ab2 # scx_loader: Add maestro scheduler
709e490456b7d4e25af02480b714074a77ba924f # scxtui: annotate an unconfigured running mode in the status panel
5c4288a970f3e0da81bee84949cf4126948aaee6 # scx_loader: consolidate scheduler state into SchedState
dccba09460f136f707f9a0ed01df353034a1d6c0 # scx_loader: emit one atomic PropertiesChanged per transition
ddd7c9df4138fd38544114df0855c722a787ced3 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
6c3cc794778e145644859fbe2b6ad56a3688ce96 # scx_loader: document the property change-signal contract
86aadba885223b3bde36060019d61351903a9e77 # scx_loader: announce the instance when the daemon starts
99eba12d5e5a5f7e822591844f573c59816087b5 # scxtui: follow scheduler state live via PropertiesChanged
a66a61193214ac79496d8c68efe4a4d3af9a7d4c # scxtui: re-ask the daemon when the kernel disputes its story
b192555d637c13c49732a74f5fe9d19ed2660026 # scxtui: pin that a pushed movement feeds the annotation
ad7cb5ef62fc2bdd8bec500c9573b34889387ac1 # scx_loader: add power-profile configuration model
6b679150901ba6fb250dd7dfb68cdf4e865f15eb # scx_loader: add pure power-profile resolution and apply-decision policy
24ccb5de30487e1a1bc54893794786081ef2c883 # scx_loader: monitor power-profiles-daemon and apply mapped modes
1295f8162fea49d5a864a76708f26e58e1c24dc7 # scx_loader: cap PPD retry backoff at the full 30s duration
b52804720c7b33e22d3cb886938f9b03492fe0b1 # scx_loader: clarify PPD monitor handle intent and reconnect logging
abac6be0593dbb5787968173be277bda959e0b8e # Document power-profiles-daemon integration
a07f4a8858309dadb8ddd793707fdc239e57685c # scx_loader: enforce the PPD mapping at the moment a scheduler appears
e689af08cbb51de1fbfaff40b88788673341cd7e # scxctl: report a coherent loader snapshot after start and switch
6856253811e5268a04510e995616e6f0515e0323 # scxctl: drop the causal success path from post-operation reporting
edeb7414cd8f0530341541653c09f608352d1884 # scxctl: restore the unconfigured-mode truth in the observation line
1eddc58d552233b69d965e28d53c3690735a76fd # scxtui: align the status annotation with scxctl's observation wording
2184bb00121798ab55cafe4cfdfb4aa40ddc7c7b # scx_loader: watch the child task to completion in the runner
8526b26fb21e070dcd13ee1f8bb5a7c2a2f3298f # scx_loader: give every scheduler spawn an identity
91a67d045221fc2b2e6283ab701b3ad35e381f42 # scx_loader: reset state and signal when the scheduler is gone for good
0d9bc5523e4475ddf45d6ae60490b5dd4ff406b4 # scx_loader: back off between respawn attempts and renew the budget
71309f40df313b5ebb34985043a12ad8cbcb5a2c # scx_loader: document the exit-path contract
f593ceb73a782f525068ee389a63311e9db17227 # scx_loader: refuse StartScheduler while a scheduler is running
7880d359a050e822d89d4df228d4320a5b587731 # scx_loader: group the mutable scheduler state under one owner
4e850d3d9119d5924b381b4883cb5039bb19a7c8 # scx_loader: move the mutable state behind its own lock
9a85169b8c9a84f57f1e820206f0f37a2ade88b5 # scx_loader: authorize before taking the state lock
1e3be22e8715c4ee8f05998d03fcb4c14b8d3d76 # scxctl/scxtui: read snapshots until two consecutive answers agree
085b8d39ae894fab4ff8666012da0ea3defc2c6f # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
