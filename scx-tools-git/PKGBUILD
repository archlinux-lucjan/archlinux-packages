# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.2.r10.gd598921
pkgrel=5
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
d3ab7ac783ebe48e299d6488017ca6960b168768 # scx_loader: Add chaos scheduler
c96848ee2d0e09f663bc75850980dbca05c83a62 # scx_loader: Add maestro scheduler
d80c9c8850c135fc998f52b28324caf7d926c847 # scx_cake: update profiles flags
02ed1904d7071368f68739e1c131d72666a5728e # scx_loader: consolidate scheduler state into SchedState
0f4724282147cb2573df907d5dcdffa6ecbf58f9 # scx_loader: emit one atomic PropertiesChanged per transition
36fc5ed4d554a3b540e9e9acd1a55d3af123a429 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
d3d5064f2d6910acf42dbfe96bfe657817986f02 # scx_loader: document the property change-signal contract
9fa349fce8429af88fe6ffa14c3a461f92fcc780 # scx_loader: announce the instance when the daemon starts
6740d919eff8f83bac3253a1274ffb1347cf99f7 # scxtui: follow scheduler state live via PropertiesChanged
42af7255bb5e6b8c3c64dfe47ddf4a6fe696c3df # scxtui: re-ask the daemon when the kernel disputes its story
e3e6e00e484a3f500e5d787a6c6f9beed72f72a8 # scx_loader: add power-profile configuration model
384f33c1614b1c41038292f5242e3ef6c805770d # scx_loader: add pure power-profile resolution and apply-decision policy
c1d2a41f33331bbc190818707663019d83e9aec7 # scx_loader: monitor power-profiles-daemon and apply mapped modes
691548e52685497f2cc2ccf9296f4af638a32e99 # scx_loader: cap PPD retry backoff at the full 30s duration
0f06adf04e480cab9ee2eeb458df40946f08a1a1 # scx_loader: clarify PPD monitor handle intent and reconnect logging
079de7dff1bdea2a2f1b43bf63365099651cf4cc # Document power-profiles-daemon integration
3d4b6c76cdddf20fbe6a234395860fbd6555c5c9 # scx_loader: enforce the PPD mapping at the moment a scheduler appears
592efe3cccc24dd510fa48f2944bcdec4b246b35 # scxctl: report a coherent loader snapshot after start and switch
7ce8afab7f7e6fb108661163859887091c9cc73c # scxctl: drop the causal success path from post-operation reporting
4c362bf60acb6795799b3c5829960bc3550dc268 # scxctl: restore the unconfigured-mode truth in the observation line
fc980504b627766282bd5b846b7ed3d0e93830fc # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
