# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.2.r10.gd598921
pkgrel=7
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
aad058607b61dbee924089fbe8deb312cf6347ba # scx_cake: update profiles flags
ccdb15b2a408a9d6350f4e3979c4c20386e4ea88 # scx_loader: Add mlfq scheduler
1f007b845909563394ae7a6bf46696f1db26368a # scx_loader: Add chaos scheduler
2e7c5bed9b760e54d3dcb93051b3f03639168e43 # scx_loader: Add maestro scheduler
dc65e14a4f4f45a1549baac8c51b71b0d0dd057a # scx_loader: consolidate scheduler state into SchedState
901904a3a7180f973b229560da7c4ffd3b54fdf9 # scx_loader: emit one atomic PropertiesChanged per transition
1940bb14c243a6633c5c32f9dd7205917fa4bc07 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
b8408b407f09999d24d65a86303dbde5cc312b08 # scx_loader: document the property change-signal contract
b174a8d392c8ceee4ac96e7d7b9b449cbf03e001 # scx_loader: announce the instance when the daemon starts
922d738d8380cba6112ad64b34c1263c419df974 # scxtui: follow scheduler state live via PropertiesChanged
36839cc7d6316c8653327b6880cd2c1af05b3828 # scxtui: re-ask the daemon when the kernel disputes its story
d695498250199a46493b938b8d7639da479fdcae # scx_loader: add power-profile configuration model
988693722320d2ea2e0fd7c180eb20e15682c160 # scx_loader: add pure power-profile resolution and apply-decision policy
d8dd8bf373fbc3ec3e665720f1082e2e49931480 # scx_loader: monitor power-profiles-daemon and apply mapped modes
b2f62b379c6999dcb7677049e02f18839ea3d9b4 # scx_loader: cap PPD retry backoff at the full 30s duration
2e13295ef296cdd6e30f769dfcc0f3107027f482 # scx_loader: clarify PPD monitor handle intent and reconnect logging
961860cc5d9f920380504c4193564e5cc584c462 # Document power-profiles-daemon integration
d9f4a4de54a21aab4dc213d94a6025503ad91d04 # scx_loader: enforce the PPD mapping at the moment a scheduler appears
f340d467931eb3a7d61e6bb55b485d6d5696a68e # scxctl: report a coherent loader snapshot after start and switch
5a6de0a695354f7f257580672bd0508e900e5479 # scxctl: drop the causal success path from post-operation reporting
5e769f73108ea50fdce13304f512d3894337ba68 # scxctl: restore the unconfigured-mode truth in the observation line
be0f47dafd171d1e9b83dc23edefb375b4007be3 # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
