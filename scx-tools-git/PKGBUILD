# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.2.r8.gcda5deb
pkgrel=19
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
c64c9d456807e035556a166a04f0b96acdaee935 # scx_loader: Add chaos scheduler
0f7e20cc40f8bda1db7bcccca91508cebfc9ce15 # scx_loader: Add maestro scheduler
7eb5c30afffc4220b7acf7ab39e3a62747e14176 # scx_cake: update profiles flags
32af2d6b570ddf63c56e24f0d1c8b21c6234c8c3 # scxctl: parse scheduler arguments as shell words
cebb738ecb1ac4d50f632d77b2bd51f045752548 # scxctl: support shell-style scheduler arguments
7e9edd00919697171c6254cb00f060aa25200c79 # scxctl: correct overclaims in argument-handling documentation
cf09b976ed3a7d37357590ae61f70ca003d52402 # scxctl: make clippy happy
12b5e502114a3d37e87630f6c99aeee16b5b097d # scxtui: query per-mode arguments instead of the configured-mode list
6acef00201eee405e119fd38323e95a11a1481c5 # scxtui: cache mode arguments, invalidate on daemon restart
ae2fd3893b1eb884a0e3cf6309b6a4949a21e189 # scxtui: preview configured mode arguments in the status panel
b3cbc23a3c0d26b2df7f3426416a3ed9ebaf292f # scxtui: render running scheduler arguments as shell words
75133f6ff83de9ff9e7d02b970f9f14fffd7ff49 # scxtui: document the mode-argument preview
d1d7a74e48c322dc5e4cbab3dca0bd2cb90f125b # scxtui: add a session-only custom-arguments field
e98ddb3d9828f9451e24ea8db76435f07eee7e57 # scx_loader: consolidate scheduler state into SchedState
a820570c2a92cca1c90a0fbb20f248eb5be25373 # scx_loader: emit one atomic PropertiesChanged per transition
1dc2a08607b37c55d58050b865d6e598f5e6231d # scx_loader: reset SchedulerMode to Auto when the scheduler stops
34c4989ae2f05b9ad1b0a1d3f7a5f22de5af475f # scx_loader: document the property change-signal contract
37c398bf948507c574b9881db7a13f5dc14d9b8a # scx_loader: announce the instance when the daemon starts
b68d7a94454e22e1a856e23b9f501614908f486a # scxtui: follow scheduler state live via PropertiesChanged
3807e792d3f225b5df5fbdb3d803f7da915f594b # scxtui: re-ask the daemon when the kernel disputes its story
d65ea00603e05a699afadc04afeaa5204700470d # scxtui: match versioned ops names that keep the scx_ prefix
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
