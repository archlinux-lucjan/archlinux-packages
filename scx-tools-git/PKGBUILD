# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.3.r2.g247fd05
pkgrel=2
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
9129803097f54a04506b17f331ab30844317ca76 # scx_loader: Add chaos scheduler
e188cbead77f7fa8bb0a0259f91c12d4b9f83554 # scx_loader: Add mavd scheduler
c0bc2d1d211a9705261b0db4f18fbcf3c7ffda9e # scx_loader: Add maestro scheduler
07ee6f5bcc88226e058d3c431b5eff60f286863a # scx_loader: Remove mavd scheduler
202aafb5df6185b7888ecdddf9121c3ee2e9e868 # scx_loader: consolidate scheduler state into SchedState
6062bf7a1b39bb306fae48c2c9418c1e02cc254b # scx_loader: emit one atomic PropertiesChanged per transition
180f07e169b36d5759d3e3f8de26bdfa7c7c28d8 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
43c722dea34a86b01f53d87f03f6b98d6c5f2a2d # scx_loader: document the property change-signal contract
09f9bfde6b07602ac89d8bfaa1aa8a5e5221ab17 # scx_loader: announce the instance when the daemon starts
e12aa94c190d9fd98f67fb288e6e7ffb3891882f # scxtui: follow scheduler state live via PropertiesChanged
3f30bbeb300b4cddab722c562e9561859f9b9855 # scxtui: re-ask the daemon when the kernel disputes its story
7249203de7ef6ea83052fc770408cc17efb06a48 # scxtui: pin that a pushed movement feeds the annotation
31879ace19402a949569b924ea0f6d690d781140 # scx_loader: add power-profile configuration model
21ef0e745529f29dca481a080a62a4718b884703 # scx_loader: add pure power-profile resolution and apply-decision policy
a2b6c3cc2e6ab381186fd4a2173872b40dbdb222 # scx_loader: monitor power-profiles-daemon and apply mapped modes
b8cf418043013766d9fc1e18426ac9369d76cd83 # scx_loader: cap PPD retry backoff at the full 30s duration
7462b304c6fb871802619aee6f411514ad37ac05 # scx_loader: clarify PPD monitor handle intent and reconnect logging
c7320eadb380e6669cb06c6b5eaa0517a1cf9c3a # Document power-profiles-daemon integration
8ffb164074daa503d4f0ca86dca427ec6fa6de13 # scx_loader: enforce the PPD mapping at the moment a scheduler appears
61f0cd35bfda78076a9de93bca3191a7cdbd66d6 # scxctl: report a coherent loader snapshot after start and switch
f732b47ca9167c4d3a8cc34133185bec2d3ee114 # scxctl: drop the causal success path from post-operation reporting
f59c35820030b2d5f9e8f4b049d7761c74a3e4f7 # scxctl: restore the unconfigured-mode truth in the observation line
30e360c1b9dae00e9790d9e819a11a5abeedfe94 # scxtui: align the status annotation with scxctl's observation wording
cd34f9b17a2f4ea857d4694d9cc77502fc5b92ae # scx_loader: watch the child task to completion in the runner
54a601ac3f32d4dd02e2d8cd70817aba129ed488 # scx_loader: give every scheduler spawn an identity
d350cf01edd23eca68298fb90a8dfd0442e8f922 # scx_loader: reset state and signal when the scheduler is gone for good
efb1550ea1c71036ff45da5ef6b8d7c6270aa089 # scx_loader: back off between respawn attempts and renew the budget
2ea5f3e78d8e7be80e0e259505091aab783e9790 # scx_loader: document the exit-path contract
6a4111b75fa20c0be2f1b79986db7f1861fe3a2d # scx_loader: refuse StartScheduler while a scheduler is running
0c5f5169424603a3815f3a81503c472f2f2259a5 # scx_loader: group the mutable scheduler state under one owner
2a92fd904dd77c2c179eae139ecdb15ea03faf19 # scx_loader: move the mutable state behind its own lock
8e451c13f095589ec26f8836b9cc1f4231d2abb1 # scx_loader: authorize before taking the state lock
6ae8716150477fe46231b3abc3aafdd98b3b89c6 # scxctl/scxtui: read snapshots until two consecutive answers agree
24875d62ea3a736869c665bc102d9fca025bf1ca # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
