# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.2.r11.g368845e
pkgrel=1
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
9ddae559367c2e3949ae9edc271b31d1506bc961 # scx_loader: Add chaos scheduler
f29a29c1f44fe694ee94d9e2102029c3e692a2b8 # scx_loader: Add maestro scheduler
14d82800f8bc04d6628f67ca2299f4a3b095cada # scx_loader: consolidate scheduler state into SchedState
04fee686cb537e6cce7d58bbd0719e3da6b0dcb9 # scx_loader: emit one atomic PropertiesChanged per transition
d1b7d8a53f8e9c78a9894c26f726d2a3258cbad8 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
e8991116a0ea1c7a26c41b877f03d42d27130f8b # scx_loader: document the property change-signal contract
6864e90a95266ede113aee9978bb6337671065ed # scx_loader: announce the instance when the daemon starts
b111a0a993b764d0b293008a6bab2142cc11067f # scxtui: follow scheduler state live via PropertiesChanged
e7e7190b5b20f47fd53ed54ad49f210a7f8353b2 # scxtui: re-ask the daemon when the kernel disputes its story
2dcfde850fcc90aba22a77a4fbf48cc3f7a61502 # scx_loader: add power-profile configuration model
b7759e48e909dde036bac9af5c82dc308f25b77d # scx_loader: add pure power-profile resolution and apply-decision policy
6b6b5de7f1d4d7fd2dc603478944f2fe996beaf1 # scx_loader: monitor power-profiles-daemon and apply mapped modes
399c66ee6df8187161a155c3bad18354edae407d # scx_loader: cap PPD retry backoff at the full 30s duration
a6f0159b39a8a15c3d5aaae288be0c7bc31a895d # scx_loader: clarify PPD monitor handle intent and reconnect logging
0484f628ffa98291f7441dffbc06001c0f7b9f65 # Document power-profiles-daemon integration
09c404e15c614bbed254b887a796808de90561c3 # scx_loader: enforce the PPD mapping at the moment a scheduler appears
230f50ea0b5c624a429135e93ded76dee7823e5b # scxctl: report a coherent loader snapshot after start and switch
2f12d4755dfad5776a71fcf82dca9ca3194f8407 # scxctl: drop the causal success path from post-operation reporting
96ef53f355fa86b6bc1ab32cc7b4f81cdbd370c0 # scxctl: restore the unconfigured-mode truth in the observation line
7f68108d4eca6e3e8524d5907e5d868d93999462 # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
