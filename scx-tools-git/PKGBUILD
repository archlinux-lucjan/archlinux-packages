# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.3.r0.geab0ea0
pkgrel=6
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
a3deac00f679804122796cddf2529c2d51b2700d # scx_loader: Add cidland scheduler
dc2b63e2292f80349def53f182ced2a887c8ba86 # scx_loader: Add chaos scheduler
1b473daead759c236eeec662365f6127a3326ab2 # scx_loader: Add maestro scheduler
a6951b577cc5871e79a6fd34d3eb696a7f01e8e0 # scxtui: annotate an unconfigured running mode in the status panel
9a42af0dbbe886ccb5d0ebd0f672ff2a3d4f127d # scx_loader: consolidate scheduler state into SchedState
cdf590daebd381c9f388026045835c642852e46f # scx_loader: emit one atomic PropertiesChanged per transition
ec4db20bdb61a3eda2f1265583b88e362cc1911c # scx_loader: reset SchedulerMode to Auto when the scheduler stops
79fe88cdabe71ad15fea689e0b35a243a6ba6c01 # scx_loader: document the property change-signal contract
35b3dc5f4f6945db49e04b829584ac01fc1dda86 # scx_loader: announce the instance when the daemon starts
d52fa44566fb414a721596fb51662f5bb0231ac6 # scxtui: follow scheduler state live via PropertiesChanged
9271265d47d102f5907616caee174715cccf17bf # scxtui: re-ask the daemon when the kernel disputes its story
981c0a9ce603ed8df7197c4819c583c22fb4b36b # scxtui: pin that a pushed movement feeds the annotation
376d5ba9e2f164934b29766ebb46f2383eb1cc71 # scx_loader: add power-profile configuration model
85377ca43e6f69b269bd7c31fbc4fdfa257b4cf8 # scx_loader: add pure power-profile resolution and apply-decision policy
e6611bab5b3516fa3e94c1056c959aa965fdc18a # scx_loader: monitor power-profiles-daemon and apply mapped modes
f29365ba43096df900b45fec9b44d8ecfbad2006 # scx_loader: cap PPD retry backoff at the full 30s duration
5d1c5813c9803051a809e2a1b4d01d5ad1ef8994 # scx_loader: clarify PPD monitor handle intent and reconnect logging
1922e2779b7283cf77f8335a501ef5f31fb0d4d7 # Document power-profiles-daemon integration
84a53fbf4fb05d3db3548b90e16504c2f6812408 # scx_loader: enforce the PPD mapping at the moment a scheduler appears
abba670db3855d709d9761024438f416557aa166 # scxctl: report a coherent loader snapshot after start and switch
b23db2ef7287d1a36df7f15d7d7435ec262be9b9 # scxctl: drop the causal success path from post-operation reporting
405166b47e4b233d931974e616ffbcaa1b5e0870 # scxctl: restore the unconfigured-mode truth in the observation line
d71614379f7018397b58acb838b6d77211ea9cf4 # scx_loader: watch the child task to completion in the runner
75eb3b21245a4b0688f1a3b53d051668a697c5b4 # scx_loader: give every scheduler spawn an identity
eb0add2644b7108eaa8491766d7f1fd4048662ac # scx_loader: reset state and signal when the scheduler is gone for good
de89e059312f01150c8f300c5ddfd83b31bdd0fc # scx_loader: back off between respawn attempts and renew the budget
84f139994127d136c5f4b02c406c7d51035ddc9c # scx_loader: document the exit-path contract
9711656d994a19e03e8cb0d41194698f4efa872b # scx_loader: refuse StartScheduler while a scheduler is running
621c3872a2ddd05d7b3f013b2f46ca6c2d41e0b8 # scx_loader: group the mutable scheduler state under one owner
199386622d8c72e50b4beebbd3927a1c8b4fc0e0 # scx_loader: move the mutable state behind its own lock
f75fd5b92d1a5bbab79ad9c884090bb960f118d5 # scx_loader: authorize before taking the state lock
e4e3f265452b5be1d50588ae893969abd88265f1 # scxctl/scxtui: read snapshots until two consecutive answers agree
b808962d6078299c1508cd326ae589c8b81da17c # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
