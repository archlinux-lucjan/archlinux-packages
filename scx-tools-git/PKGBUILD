# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.3.r1.g30e64eb
pkgrel=1
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
8fe5854cbe541b3c4b89edb864bd29c28179652e # scx_loader: Add cidland scheduler
566a5c92df90523b46f7da305a4d526c6d0265d1 # scx_loader: Add chaos scheduler
11c29d6109b037eb262bc85be7450dd7aa6070d2 # scx_loader: Add maestro scheduler
09544150a430cb608cb75282409afe46b63fca22 # scx_loader: consolidate scheduler state into SchedState
481b67f16310fb4f4a3dfdacaef9794dca24d5ee # scx_loader: emit one atomic PropertiesChanged per transition
7c516293651a6418223b6823aeb45bdd8cf9beae # scx_loader: reset SchedulerMode to Auto when the scheduler stops
7846afb38c03d64c960ad40a9f783b4d2f8699fe # scx_loader: document the property change-signal contract
b8c70e5a06f422b8c6dff62d5f6fa05e691e0eef # scx_loader: announce the instance when the daemon starts
917de3aed2efd7c8357123dcc821f9b473277923 # scxtui: follow scheduler state live via PropertiesChanged
49e746298f69df17e3cfcd44a7ffa1a79b123778 # scxtui: re-ask the daemon when the kernel disputes its story
4f245d93b8645e535113b77058a3b1208b431657 # scxtui: pin that a pushed movement feeds the annotation
f3dd4ddf546fede3aa9f9145d50d5f5fd1a3d9f3 # scx_loader: add power-profile configuration model
34de5df0f268c65e2956916a9f7529813f98c23d # scx_loader: add pure power-profile resolution and apply-decision policy
821595e66cfc49084ede0522e88065b484504e11 # scx_loader: monitor power-profiles-daemon and apply mapped modes
ed20cc458d2c9e998d22998e99c4008d5f11668e # scx_loader: cap PPD retry backoff at the full 30s duration
5360a8538b0f41f09d67deb4f22e1e2790f45afc # scx_loader: clarify PPD monitor handle intent and reconnect logging
ea2caaf35265e924bc8f6e495e4c6af1f2af202d # Document power-profiles-daemon integration
f9365b517543e98c8d2bfe9bcf62c4d564002d59 # scx_loader: enforce the PPD mapping at the moment a scheduler appears
0241b79968e018246bb65c69b4f35c3b724b8037 # scxctl: report a coherent loader snapshot after start and switch
e081beff79dc611c571ce0e8078d6da3eb3accf0 # scxctl: drop the causal success path from post-operation reporting
4543a7539d8fe5417cf33dbe01cc98b71b4aebfe # scxctl: restore the unconfigured-mode truth in the observation line
e6d15bc4fb977a64254562c355eb6bb6b8b8559b # scxtui: align the status annotation with scxctl's observation wording
22a982aa1b5deddfc22b9a466c63c07ed31f76c7 # scx_loader: watch the child task to completion in the runner
a4cc7d75884e321e1211268d592b45ddf9c5f234 # scx_loader: give every scheduler spawn an identity
b1501e8521004e20a0b072ef11dff356ddf7d17a # scx_loader: reset state and signal when the scheduler is gone for good
cb4b7da1344d2338dd15abee617819e3349f6c18 # scx_loader: back off between respawn attempts and renew the budget
430c7674048993c94d946771d68f81078a9668b7 # scx_loader: document the exit-path contract
8f4f684eedf56fb186c72ac8c2d1860f69290694 # scx_loader: refuse StartScheduler while a scheduler is running
37c80e939450539ec32f1e4b1c004ab011644c66 # scx_loader: group the mutable scheduler state under one owner
10ac16a58144e7b39eaff8bdf53b90c0b65b7e57 # scx_loader: move the mutable state behind its own lock
1e371c59b00434184b30691b8b2c1cac36940252 # scx_loader: authorize before taking the state lock
9c90fbb7865f37e1fab28609e0d94bf4cdaee71c # scxctl/scxtui: read snapshots until two consecutive answers agree
4126deaae8500e905217e121bbfb6a3ada55991b # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
