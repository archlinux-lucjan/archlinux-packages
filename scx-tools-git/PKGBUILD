# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.2.r10.gd598921
pkgrel=3
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
d3ab7ac783ebe48e299d6488017ca6960b168768 # scx_loader: Add chaos scheduler
c96848ee2d0e09f663bc75850980dbca05c83a62 # scx_loader: Add maestro scheduler
d80c9c8850c135fc998f52b28324caf7d926c847 # scx_cake: update profiles flags
3f2cadf564c7cc9c0990ef8d3c3071610b044a04 # scx_loader: consolidate scheduler state into SchedState
03b19b5ce6a94cedfa199e0a70c4faa04dcfe380 # scx_loader: emit one atomic PropertiesChanged per transition
95afee4b3f617c760df232bde848cc6d8318c916 # scx_loader: reset SchedulerMode to Auto when the scheduler stops
7ac10146d214bed0b760d07d6f864c4bc4a83dc2 # scx_loader: document the property change-signal contract
97bbc6e3bf1d9eaf1eda7492ee3bd87554f640d2 # scx_loader: announce the instance when the daemon starts
5a45cad7f31884b534d8c88eb36ba7ceadd9935a # scxtui: follow scheduler state live via PropertiesChanged
dc0cbe56e04f3734cbb3e5d72dba9b4902c8c90f # scxtui: re-ask the daemon when the kernel disputes its story
662a8b4f5f800fa1882206ec4b51c9d9c326e7ec # scx_loader: add power-profile configuration model
11c365338a90e28c09c8c059bcb182b72a523f05 # scx_loader: add pure power-profile resolution and apply-decision policy
8b4d68119863756966372890466ea4f809c0fe17 # scx_loader: monitor power-profiles-daemon and apply mapped modes
9da5324f3f75c850e20d95a4883ddd0f17486a33 # scx_loader: cap PPD retry backoff at the full 30s duration
e88ba03ba3f73c0695f092b5b8f6ab67f477b66d # scx_loader: clarify PPD monitor handle intent and reconnect logging
5810cf09b68bacd4cc4d95fb4d5efa97a5fef925 # Document power-profiles-daemon integration
111fdf40f52e2d96e49d687b299032dfa77a3588 # scx_loader: enforce the PPD mapping at the moment a scheduler appears
222baf618c24050de4e167a7285719fcbcdd0ed0 # scxctl: report a coherent loader snapshot after start and switch
837f6efafbbad33f9a7ecf65ef93aacf794e5f98 # scxctl: drop the causal success path from post-operation reporting
17a959938085b8b9cde4004864cbf1195bc77610 # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
