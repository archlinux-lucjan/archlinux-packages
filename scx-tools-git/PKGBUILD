# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx-loader/blob/main/Cargo.toml
_mode=release

pkgname=scx-tools-git
_gitname=scx-loader
pkgver=1.1.3.r0.geab0ea0
pkgrel=7
epoch=1
pkgdesc='scx_loader: A DBUS Interface for Managing sched_ext Schedulers'
url='https://github.com/sched-ext/scx-loader'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  polkit
  scx-scheds
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
options=(!lto)
provides=("scxctl" "scx-tools")
conflicts=("scxctl" "scx-tools")
source=("git+https://github.com/sched-ext/scx-loader")
sha256sums=('SKIP')

_backports=(
a3deac00f679804122796cddf2529c2d51b2700d # scx_loader: Add cidland scheduler
dc2b63e2292f80349def53f182ced2a887c8ba86 # scx_loader: Add chaos scheduler
1b473daead759c236eeec662365f6127a3326ab2 # scx_loader: Add maestro scheduler
bc903b27e8c4aa0ffb0f144e3d63e277a6ac8de7 # scxtui: annotate an unconfigured running mode in the status panel
a2b4d951febca2a993d571855dd362429a7ec479 # scx_loader: consolidate scheduler state into SchedState
1dc757539cb26e8470c4b67dbadf7fa2b78964f7 # scx_loader: emit one atomic PropertiesChanged per transition
ee812568bb6d6ea3c2a812cfc2f822dfa771c91f # scx_loader: reset SchedulerMode to Auto when the scheduler stops
569b9c4942efa3c5135c9b8d666e47b67f47c2f3 # scx_loader: document the property change-signal contract
1d54b9ed4609acf6cc3a06900e66f7bda850dd8d # scx_loader: announce the instance when the daemon starts
5f783ecf0c0f7bb454e3ddfd06faaac9d375ade9 # scxtui: follow scheduler state live via PropertiesChanged
022a2470325bd874a2a7352043ce79bd7580ae39 # scxtui: re-ask the daemon when the kernel disputes its story
d8353219f7637125066ba27eb2a214f1076eb264 # scxtui: pin that a pushed movement feeds the annotation
79bd21f0178b8186b14c6348a238d5e82695c4d3 # scx_loader: add power-profile configuration model
d28a2a85688e7aea8b7357d6301ae0e7e605877b # scx_loader: add pure power-profile resolution and apply-decision policy
ff86561cf36893d719dc8d8d605daeaceb737827 # scx_loader: monitor power-profiles-daemon and apply mapped modes
becede09679751d9799b3a5a79c3da16a1a7ca04 # scx_loader: cap PPD retry backoff at the full 30s duration
5a1fe03e0a2cd8625505008836de4eb226ddfcba # scx_loader: clarify PPD monitor handle intent and reconnect logging
9e010986f8ab6fb9478ec36699ecad7b3e0efe6b # Document power-profiles-daemon integration
a60dfe48e0fdb0ff9cd9db50bbdfd94d848a7d2e # scx_loader: enforce the PPD mapping at the moment a scheduler appears
2874e5b20732c76c590dc6dd90b0f010bf73cf18 # scxctl: report a coherent loader snapshot after start and switch
b8dfd62dbf14c8c1a9b150b54c1b31689fa13203 # scxctl: drop the causal success path from post-operation reporting
7e54a680b6ce7da798171e1093e16936de6bd2a0 # scxctl: restore the unconfigured-mode truth in the observation line
1ae0c40d532811e381e81aad674d6af339b17e05 # scxtui: align the status annotation with scxctl's observation wording
1146ff8b421142524c13c9eedf1f13d5ab80b613 # scx_loader: watch the child task to completion in the runner
90b121d284216a6f87dc1a58bd75b51a0a296fc1 # scx_loader: give every scheduler spawn an identity
93100c1e9d58da46950837192c044b4542a1a436 # scx_loader: reset state and signal when the scheduler is gone for good
56a35dd5e0f45b3266220e11a89db29d307bfac3 # scx_loader: back off between respawn attempts and renew the budget
8756c8023b9a88f7bfa1a1ec6bc786f13efda97e # scx_loader: document the exit-path contract
b5ce42a1eba9792e7be79a5f056442f1e9914667 # scx_loader: refuse StartScheduler while a scheduler is running
716074e612ac6f4989366c0493307aaa15d11db5 # scx_loader: group the mutable scheduler state under one owner
0d5e3f767337f944f363bf2004fc9a0f738a55ef # scx_loader: move the mutable state behind its own lock
2f77d96365968824fcde678d2613eb49e2b43f53 # scx_loader: authorize before taking the state lock
f01a95fadccb11c2fd7237bb07dd0d6157801257 # scxctl/scxtui: read snapshots until two consecutive answers agree
97927cb7a45a0a2b34e9f244b7576995338172f2 # scx_loader: log the selected configuration source
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build --profile=$_mode --frozen --workspace
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' ! -name 'xtask' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +

  # Install runtime assets via xtask
  # (systemd units, D-Bus services, configs, sample files)
  ./target/$_mode/xtask install --destdir "$pkgdir"
}
