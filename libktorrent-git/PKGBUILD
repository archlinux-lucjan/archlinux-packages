# $Id$
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Antonio Rojas <arojas@archlinux.org>
# Contributor: Ronald van Haren <ronald.archlinux.org>
# Contributor: Andrea Scarpino <andrea@archlinux.org>

pkgname=libktorrent-git
_pkgname=libktorrent
pkgver=2.1.0.1.g70e2e90
pkgrel=2
pkgdesc="A BitTorrent protocol implementation"
arch=(x86_64)
url="https://www.kde.org/applications/internet/ktorrent/"
license=(GPL2)
depends=(kio qca-qt5)
makedepends=(extra-cmake-modules python boost doxygen git)
conflicts=('libktorrent')
provides=('libktorrent')
source=('git://anongit.kde.org/libktorrent')
sha1sums=('SKIP')

pkgver() {
  cd $_pkgname
  git describe  --long | sed 's/^v//;s/-/./g'
}

build() {
  cmake -B build -S $_pkgname
  cmake --build build
}

package() {
  DESTDIR="$pkgdir" cmake --install build
}
