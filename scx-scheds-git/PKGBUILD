# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r322.gc0acb76f
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
90954e8bddd08d51b912afa2ed28a8a073138294 # scx_cake: Sync Frontier optimizations onto latest upstream main
9093fbf9279e114fd2b6b2742ecef03eb9a11a93 # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
eee4b971eebeb0e0bf64179e59d1ac2d81f5d119 # Revert out-of-scope Cargo.toml change
8e92da9928959f63bb325105d29760da461e86c2 # bpf: scx_cake: Implement BPF-native clock cache
955a1ea178c5688bdc4729c112bd4221c578df7a # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
9368b252842f095a90fa97bd47cd7391d26678f0 # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
5b496a4e0d854c63edb959433db5497bb9141da1 # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
ca46823271fd5822dda3ed2f4546efa2b68ef7e3 # scx_pandemonium: update to v5.6.0
98e56df52fb501cb46577c855d931d18a9de0450 # scx_pandemonium: fix verifier bounds for older kernels, restore veristat baseline
43ebbcea32551ea6a5c404c35278ecfad05ca985 # scheds/experimental: add scx_flow v2.2.0 scheduler
be41a3ece4aa3dabcdc28674d4b42ea16b45d296 # scheds: Introduce scx_maestro
fcbe743a8e162cbb73c9ebdf3021da5f6867b797 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
2f50b207ee9b3a1a313eebb379fc8d2e94789fd8 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
afd032673889b3e381e7ff2a5d7d9d9191de19db # scx_maestro: Don't update deadline in case of re-enqueue
34fdbde9fe6d6cd8b2fc14d8403d3e16d865a73f # scx_maestro: Introduce completely fair scheduling
5802d62385b705923ef0881867a70c02c4e491b1 # Cargo: update libbpf to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
