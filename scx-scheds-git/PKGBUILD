# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r87.gfa3ec0b93
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
9d152c8c82b8a80f683594af7967760c9b94b649 # scheds: Introduce scx_maestro
774ef44044629fc7675cf3d9d18a86535beaf8cd # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
b75320f2da7c1825550ff6a1d873f87042b13d95 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
364f8f65e07ce3c738016941c78499d5f4222bfa # scx_maestro: Don't update deadline in case of re-enqueue
96c561b81e4fd47078ff69a7d1b60c512c089ac6 # scx_maestro: Introduce completely fair scheduling
51481a798c70d267855872d3d16d2b8a14b84919 # scx_maestro: Make enqueue + CPU kick more reliable
d854630bb6d0f00242930b8a20ee950e69b15ea6 # scx_maestro: Enforce preemption each tick on CPU contention
3fd310d8a2311c051dadb6281f0e46af62167360 # scx_maestro: Temporarily drop sub-sched support
2a693cb0653aebcb16c7ce09d51d5034a2506f83 # scx_maestro: bump to 1.1.1
ad0042d148dcc82119644b650367e5557c1c6c91 # scx_maestro: fix formatting
5278e10951b92892d6eacfe3c92dab1124d04e70 # scx_maestro: bump to 1.1.2
bee88bd3aee0c09448bc8c7f655751b6144afaad # scx_maestro: Use scx_bpf_task_set_slice() setter
53f63898694e5cc09b981a775135a435ea8cfa9c # scx_maestro: bump to 1.1.3
aa96886fd6e8574b2fdcc01d201ed9f6fde05ad9 # scx_maestro: bump libbpf to 0.27.0
0d86e497f1d7e0c5f3e50aad1e8f318971579cde # scx_mlfq: bump to 1.3.5
f5e4a1227493f426ba33f6f5ac3eb31ad75222c7 # scx_mlfq: bump to 1.3.10
77e1ccb638d87c9c34ceec08a48505af8770e342 # scx_mlfq: bump to 1.3.11
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
