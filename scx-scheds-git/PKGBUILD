# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r441.g4969ac48b
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
b41b5f5c745f6f4b1bdc1d2cf0ca48961339c46b # scheds: Introduce scx_maestro
bda272896ff560f48590448ad487ed8f1ce53acb # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
597e47b1c2e19cd4ba12ffbee000dbfc916df36c # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
1803a1329d4898a3e7009cb0531c85c9531d4440 # scx_maestro: Don't update deadline in case of re-enqueue
a58994702b59e9a4733c7d14656f93e65967e31a # scx_maestro: Introduce completely fair scheduling
514322e080ccfccfba8565387874d0ba9f3302b7 # scx_maestro: Make enqueue + CPU kick more reliable
9a3e9bb958405463a6b33a344d405071866e2473 # scx_maestro: Enforce preemption each tick on CPU contention
a108ea4fe384072a6769e0ef43183a2135caf5b8 # scx_maestro: Temporarily drop sub-sched support
69af5dbbf4595d500895393e8982538ff960accc # scx_maestro: bump to 1.1.1
49a4b67e03dfeb0a49a2294f396cb2c55aa776f0 # scx_maestro: fix formatting
ac6d887bc260621bde4c02d559d6b176d774614e # scx_maestro: bump to 1.1.2
3eb96295a767abbf100c6be46f2271095662174c # scx_maestro: Use scx_bpf_task_set_slice() setter
2dfc69d98efa95a5d3f0368b52828e54b04559bc # scx_maestro: bump to 1.1.3
e3dd350ad8a88975cea10821a03ba0d48dd0bf59 # scx_maestro: bump libbpf to 0.27.0
8c977ff5eeb084bde686e5bb56e5b0ce879ebb9c # scx_maestro: migrate to Rust edition 2024 and rust-version 1.91
8f6d62b20fd7b49b2c36ab3b876ae81860830b7f # scx_maestro: fix formatting
8a3ecdfdc5a767e286ec65aefc2d4ab24aa49f8f # scx_maestro: bump libbpf to 0.27.1
c311a25970e3c3d6eff45f1ced611ac703bc4fad # scx_pandemonium: bump to 5.21.0
fc9cf7bee1f021b81fd45325c5fbd2f829b6c24e # scx_pandemonium: bump to 5.22.0
c22f6bb5b09ec6f579983a8a23c093b380d4ed1c # scx_flow: bump to 4.2.41-o1
e363036508233380284fa06a477ad893fe510fe7 # scx_flow: bump to 4.2.46-o1
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
