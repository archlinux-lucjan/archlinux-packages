# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.1.r199.g7632c961
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
83a79dfbe76b0a194dd7a53eb19780bc62798ee4 # scx_cake 1.2.0: hybrid-queue champion (LLC-vtime + hybrid4 game-tuned defaults)
0c150ea9839d04988df45488564a19ea61286419 # scx_cake: fix watchdog stall at source — drop llc_pending bitmap (Option A), remove rescue bucket
77db1b7ccb5650a7560407c4028854cc977e689b # scx_cake: silence build warnings + cargo fmt/clippy (no functional change)
b1934b77280574d35d6a828124e93099acdd3d22 # scx_cake: remove global core_steal_pending_mask bucket (per-CPU truth)
a045c4cde7bd639c8cbc750d486c0ae9372d2686 # scx_cake: silence debug-build -Wunused-function on config-gated helpers
8a5802c644e4107751778b552b8b6d416c96e9b1 # scx_cake: promote vtime-local nightly candidate
5df3a83fb755f3b5bc82d1f55cab2f837f4c9365 # scx_cake: fix formatting
53089bbdad31d892ab2b4c512b2fd721def52af6 # scheds: Introduce scx_maestro
e54ac0fdfdbc019326de40da64d15431ba9f8d50 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
dc73aee76755b2728f4bb8cec7c814ff1795da3b # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
f7610adbe38820803b014119c1bb383c72d10a7a # scx_maestro: Don't update deadline in case of re-enqueue
4e751cf1cae86ec3b9875fef4d55fea99f3095a5 # scx_maestro: Introduce completely fair scheduling
8ce8e8ce62a06410490f248818268b5cb508ed43 # scx_maestro: Make enqueue + CPU kick more reliable
087abd6b1da41280d17bfaacd8fc0043e2653538 # scx_maestro: Enforce preemption each tick on CPU contention
382993907095c980811257336f5d7059f288f463 # scx_maestro: Temporarily drop sub-sched support
c777686a4200ff3b07a22a73c06e1d7530e10e45 # scx_maestro: bump to 1.1.1
06a1c75dea0c9f94f32bbe69e8ba146547ea5a69 # scx_maestro: fix formatting
9eb3547b52b71a48e397cdcebea5d5e3f0ae3b46 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
