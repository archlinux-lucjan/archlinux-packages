# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r183.g610db46d
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
01a9751bf7e9e91787768239f50c36c9ed6cac37 # feat(scaling): compile-time hardware detection and gate system
5749069cd22d58b87986a532740d92e6d89b831b # fix: sentinel collision at 256 CPUs, gate2 fallthrough, libbpf logging
6921834a08e8b129fdd7057edac85d475525d921 # cake/bpf: assembly-guided instruction reduction (-35 BPF insns, -3.6%)
f8624e94e9b313df108d05407a8bdcc286748440 # cake/bpf: frontier hot-path optimization (-9 BPF insns unconditional core)
25fbe8fc82e94e8d413af8685ab97a7d4f0df289 # scx_cake: comprehensive comment cleanup + README coverage expansion
a2b1b83f0edef4e2042117f399157034b0bc2538 # testing performance classification changes
91daffdb72148df29a106b03afddfae8aceb81c6 # bpf: execution path flattening — hot path 504→409 insns (−18.8%)
baf40975f876a86ceb3d9e92f8e7e72d3dd4fe4f # scx_cake: Implement Phase 10-13 Hot Path Vectorization Sweeps
94173e3b9df81a7f5adf70ce8188057c9135ed89 # scx_cake: Implement ALPHADEV Phase 14 O(1) Kfunc Kick Bypass
ead9c458a1145c917b6f1304feabbd42b23c3135 # feat: game detection ACF manifest overhaul, stable-logging, and origin-aware kick guard
7036ab4481c7d271f1a2307c6a4fb96ab14505cf # fix: stage missing bpf_compat.h header for atomic macros
0dab5aa92547bd00c19d319af49ec2776a948448 # fix: remove child_count threshold for Steam games to enable native linux multithreaded detection
b54e02050eeaf6d86d20afe8eda6ad4a20d1aa59 # cargo: fix formatting
76208a4e7b15cca5eaa3ae91602ebb4b5527d2d0 # Cargo: update dependencies to latest versions
455fed56ba634da676d2362d9590def6eb863531 # Cargo: update libbpf to latest versions
0903828a52d5fdc1c45010813a66f30d1a034531 # sched: add scx_timely scheduler
6be6dc7240afeb71ea771bceede3fad0afdac918 # sched: add scx_timely license link
1886c2e173edeba6b9790cdb540f1c49e6fcbcb4 # scx_utils: Handle ops.sub_cgroup_id in older kernels
51619164aef77def3ebf69d018fe1180f27ab66e # scheds: Introduce scx_maestro
e9cc3a7017c14dc3adcf2d98687f8d2ffaadcf11 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
5585a9edeba541464c36ea73a8b012507fee96d9 # scx_maestro: Add better NUMA support
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
