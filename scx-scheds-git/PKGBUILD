# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r274.g7c09a883
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
4c00f7abec6d07037135051f636874f3d5b5d9a0 # scx_cake: Sync Frontier optimizations onto latest upstream main
266d406c6623d47f0c61e362e4f07cbc85e01005 # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
188ca054da358c5c32d196195ae494bc42e85f10 # Revert out-of-scope Cargo.toml change
6b19641cb3efa396cc451d055138224a310a2df8 # bpf: scx_cake: Implement BPF-native clock cache
31b3629efcced3622bccb35f480cf020fe4ef656 # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
e9a177b784fa4d83e5bfda9ce5e69be2d82c2f1e # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
f45ff315f5a177fa916cc5e0465a98f423c4123e # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
14679e352f59a9ccd52596bba3276c8b55dc0a6b # Cargo: update dependencies to latest versions
815d24f5de68884f24a05c33c77013ba8143dfdb # scheds: Introduce scx_maestro
de58110ae07cad46c3d3117e490d15afcb69be86 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
58715960a91e0704d24ebb92ef7ddbbc9d42257a # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
ffee15eba9e18c6bf1df2045616e8abb26844ad1 # scx_maestro: Don't update deadline in case of re-enqueue
f3a448c95edf8454a95c5fd59f9374cf368f3ad6 # scx_maestro: Introduce completely fair scheduling
74d949a022e743e1b7b12d5dfa6b5c9e6502cd99 # scx_pandemonium: add PANDEMONIUM scheduler v5.6.0
90f627861909cf3d0f91f725015a8fd944311ed4 # scx_pandemonium: add veristat baseline, use scx_utils build_id
d751cf346897befb92a8b7853c072cd2c55703be # scx_pandemonium: add veristat baseline, scx_utils build_id, ops version suffix
43441f3b6df7a4a1f36266103c11753cf01115a1 # Add scx_flow scheduler
97e882cf6bc8f68ebac5af7a18b65d359f04853a # scx_flow: suppress generated vmlinux.h warning noise
d2a2f82c2f771d9619eb715587573a553401049f # scx_flow: report full build version
3a39a60c15f7667bbfd57be118e767f6d7b5d987 # scx_flow: use relative LICENSE symlink
210ae2e5d5e91705b0ab9d07e49ef6ee68ff430a # scx_flow: simplify normal exit logging
afaeca8b7c485d3a5b304883efc21ff75d57d12c # scx_flow: add v2 cpu-stability bias groundwork
d8ca1988f535b4b34ab6962d7a47f055f3cd5ddb # scx_flow: add v2 latency lane and bounded hog containment
a8d9c1c433d7474ebdd4ce38d4c3ee22a5ae5347 # scx_flow: tighten v2 hog containment thresholds
a14f6e3d533cc39bea27260ff56ddb4eacd5ded4 # scx_flow: widen v2 latency-lane admission
171eb69a1fcb4e7ccb64fd1aca6c99c541851f13 # scx_flow: add v2 latency routing diagnostics
72c1704e3d347eec488ea2c44a4ed05980ed57bc # scx_flow: keep contained hogs off idle local fast path
f95826a91ae3395dc8e35e65df480f012e854a9b # scx_flow: freeze v2 lane baseline
d673a9b05fbecce39a2c115a9ec57431dd5772e8 # scx_flow: checkpoint v2 repeat-validated baseline
ecc211ca0e29f85927c2cde03f2b5a32766b33c0 # scx_flow: polish v2 release and make policy state per-cpu
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
