# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r338.g37bf708c
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
f5b14d8dc773f698fc2565ab5e73b293d99ca4a9 # scx_cake: Sync Frontier optimizations onto latest upstream main
3b0c7df89e299b141a6f0d49012c2d20a33d9e44 # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
fa105bc482a643d3b70802da9814dd798275a420 # Revert out-of-scope Cargo.toml change
cdcd97ad5af3c9fa77a952c2f08b5a9c9b0db2fc # bpf: scx_cake: Implement BPF-native clock cache
0fd9eb090f9abffe2135cff8f7719f41cb8f49d5 # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
9aa458d62f04c3d314606e1dab85a2b00342235a # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
f8ac5261ae1f61fc5c908214f05824b53b8e028a # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
9c85c6efca0fedb8a4fbf898a698ba4424c0aa74 # scx_cake: ALPHADEV Phase 4 & Phase 9 Architectural Flattening
fde6e4645e5453d65e92016a1bd0e880f2aedb91 # scx_pandemonium: update to v5.6.0
97c21238a9887fb2ccb1cddef895d7fa6ae25db0 # scx_pandemonium: fix verifier bounds for older kernels, restore veristat baseline
16220b9227caa0f91975f4d699210082622da507 # scheds/experimental: add scx_flow v2.2.0 scheduler
9eec22fc9099a3ee1cd2496389ec9193a3843007 # scheds: Introduce scx_maestro
c0cc2c71e94c42ea02a4f92e4308979aa0458435 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
39851c4cbca677939bf3bfd6d37f0c3d924bb477 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
1e0ba0d0e04eb401a5df8f10305621bd86e51b04 # scx_maestro: Don't update deadline in case of re-enqueue
737b2c1cad1d05e7263b9cb32ad1dfbfda7fcd5d # scx_maestro: Introduce completely fair scheduling
7550799479548117fd58fdaf0735b17b6edc6798 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
