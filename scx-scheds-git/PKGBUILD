# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r38.g17f76ac6d
pkgrel=5
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
d55208f76847a2a7a543c818dc5f998b2f6b063c # scheds: Introduce scx_maestro
ca339d4690bbf94d9e4f60c097a3a52a6f67d133 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
5ed7e3112597ebdb7ddb5607353124e4b870e431 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
fdeb54997342c9d9dbeaa714f71bdb5704dea6a3 # scx_maestro: Don't update deadline in case of re-enqueue
7160e405397c42971787b52a52e1ae055ec8e2dc # scx_maestro: Introduce completely fair scheduling
56b4daa9f9bc113823d64aa898bf004493aa1bc4 # scx_maestro: Make enqueue + CPU kick more reliable
e7b0bc66fd22de7d9bcfdfafe1dfce236691076e # scx_maestro: Enforce preemption each tick on CPU contention
e0d605bbc685eb0b49935f657dff1863325a845f # scx_maestro: Temporarily drop sub-sched support
6f63bc45a67831b2fe29eff01887d35cda02aacf # scx_maestro: bump to 1.1.1
bc0f15746562a65728195c3d55daef44231fc5dd # scx_maestro: fix formatting
70d68fc95d48dc3ba81420bb50bce4c115f5f65b # scx_maestro: bump to 1.1.2
66dc9a342663990dc44a319a0a4fccb44d1b5317 # scx_maestro: Use scx_bpf_task_set_slice() setter
f1da880b7b1ce12e9fca90cc43c124943ff726d6 # scx_maestro: bump to 1.1.3
6a479f5e059d904d4139a53c10a88bab1d27dbf0 # scx_mlfq: bump to 1.3.5
430173c4ddbccc0784362b4bf9ec773b50cceeb2 # scx_cake: select_cpu redesign — self-park mailboxes become default (2026-08-23)
102aacfe5ea3b1b526a2d4d51373a44fc1011b0d # Cargo: update dependencies to latest versions
e5e357372db618d90ae7bdfc9a443f27540a85e3 # Cargo: update libbpf to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
