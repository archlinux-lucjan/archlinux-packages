# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r336.ge3c87b1b
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
ced099d92c1d1aa88d2cebd0c521decfbb309231 # scx_cake: Sync Frontier optimizations onto latest upstream main
dec98c1f99f4a4da9acf3f834cdf0eb66491f420 # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
a9e7c481271b5fa0dfee54202c76f86e5be01dca # Revert out-of-scope Cargo.toml change
ab820c0235f13096b9825be8c6227fac02a2459f # bpf: scx_cake: Implement BPF-native clock cache
90dec3163199f6cc7293a167a411e4e394dec5e9 # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
1543acc52b21fdc32b42faee180c5a5847f951e7 # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
f0588df0417e65d12cf71d3f6207b1be6cce8a29 # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
57df47ae586600002fb697844ff1ad0302caf83c # scx_cake: ALPHADEV Phase 4 & Phase 9 Architectural Flattening
11dccdd47eba79b0edc1190bd63ca4019bdf445f # scx_pandemonium: update to v5.6.0
47b1aa7e29a9fce8a712a3edf9f9c32b845ce154 # scx_pandemonium: fix verifier bounds for older kernels, restore veristat baseline
74659da73df17ffe00353249432d5d6f69e8888b # scheds/experimental: add scx_flow v2.2.0 scheduler
39fbdd9a99d45bbf481ffd795c436729cb515de8 # scheds: Introduce scx_maestro
d7d4b649f988d3537f4b769c2ba8e0c115a254a8 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
8a656965114d79c6261de01aeb0a902bfc7cc6a2 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
15e40d79400957487db0399eec3250513f18a67e # scx_maestro: Don't update deadline in case of re-enqueue
02de897875c20633a28a8b240aa52f4137885f4f # scx_maestro: Introduce completely fair scheduling
6efe81a9b7149fe522ed6880f152244238b7dd04 # Cargo: update libbpf to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
