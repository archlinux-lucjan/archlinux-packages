# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.1.r205.ge1059502
pkgrel=3
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
3dfa7038a7600c7e2be31e94c0ee175782d3c06f # scx_cake 1.2.0: hybrid-queue champion (LLC-vtime + hybrid4 game-tuned defaults)
2a5cc3c94f1ea5067863df0de723dca96c4e825d # scx_cake: fix watchdog stall at source — drop llc_pending bitmap (Option A), remove rescue bucket
9a9e6d70c534dacc917e5bb84c475594374cfcfb # scx_cake: silence build warnings + cargo fmt/clippy (no functional change)
4ca5fc184d08a2a8a3c231686f145d0d3743b30d # scx_cake: remove global core_steal_pending_mask bucket (per-CPU truth)
323f90130e1c44681eb530a1dd32ec586e7d7a15 # scx_cake: silence debug-build -Wunused-function on config-gated helpers
06dc4477eeadf4596b655a043adc26b1bcffcdee # scx_cake: promote vtime-local nightly candidate
4e9a8c29159ac3a20c77c8a9aeb3e4abbaaafe95 # scx_cake: unify local-waiter wake (preempt-kick) + free memcpy slice; gated service-readiness instrument
5d1897b989e6a9f430f9b8f5b91737074c82210d # scx_cake: dedup shared-escape twins (unification, behavior-preserving)
09bb9148356c67e6384d9b5c9731c58df562d245 # scx_cake: EEVDF-like wake balancing for throughput class in vtime-local select_cpu
bea1e37532aba7007394bdc935d3eb55b3d7407b # scx_cake: drop redundant self-kick on local non-preempt wake
7b595a582af4c015136bb92d8b36c9160f01e48c # scx_cake: rebalance throughput class off congested prev (EEVDF wake_affine)
6dba2afb140f49e954a4621938f4c2348a7f2c0c # scx_cake: integrate SMT-aligned zero-cache-miss migrations and Arena-free hotpath
641b42d4ca87d1765d0e7488cd07a41e42a19451 # scx_cake: fix build warnings and gate telemetry functions in release
4150955edab44d2a68e2b369f6b2bb10f9cb871d # scx_cake: fix Cargo.lock
a1c5f8ca307a8335804d9d21cbd6539ef8cc5d8b # scheds: Introduce scx_maestro
8fb45e46584f70e1495f5259904df5cb22d90ed4 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
8de94ba52c410510d9c8e9779f68f3492c988865 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
9c47d7c18d0ddade4fd983628f69a13f9d65f23f # scx_maestro: Don't update deadline in case of re-enqueue
292e202389e26778ef07a1fe36b09967808a824e # scx_maestro: Introduce completely fair scheduling
41babb77d6b1e11d6b8e3bda7cbfd625b8d37eda # scx_maestro: Make enqueue + CPU kick more reliable
2f1c7743f018ecd1bd2f5d35ac5d22acce00a4ef # scx_maestro: Enforce preemption each tick on CPU contention
243992ed3fc4de02914b22b7bdc1b648125f84b8 # scx_maestro: Temporarily drop sub-sched support
0de976f7fa67a2aa52a154f799833565d2343c6d # scx_maestro: bump to 1.1.1
e805b8dedc27fd8621e9a8f70479984b87fbb964 # scx_maestro: fix formatting
c933f7c0f10c7c3ac9a2feea9f467669fa6fede4 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
