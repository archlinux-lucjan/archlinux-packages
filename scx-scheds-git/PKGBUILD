# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.2.r105.gc630d994f
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
24bf35a0c019dba24b8f5433b62216789d91dece # scx_cake: 1.2.0 — clean-slate EEVDF-parity rewrite
fa4ebfa578c0271d57aba675aa35c4c978c535e5 # scx_cake: identity-gated preempt fast paths + RT-owned CPU avoidance
22964f916c283b0f85d8331b9444a320832749bc # scx_cake 1.2.0: K+L+M+S1d checkpoint — game gate passed, sealed benchmark wins
58cb2bf7f38f2af3201ab524281e9a51097ab136 # scx_cake: G20-G26 campaign checkpoint -- first frame win over native EEVDF
f49e1a2918e0f81d670033e058d87f67957b142b # scheds: Introduce scx_maestro
e5990cc72774f53d2968939e83df4be0848cc7d6 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
a1641e86fbcfde9018004a9b680ec135f170e349 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
4c4b731568c8472122b418af6a7b3e6b2db76733 # scx_maestro: Don't update deadline in case of re-enqueue
2db73522f1396957864706567b1539689d396821 # scx_maestro: Introduce completely fair scheduling
3b0a0090dc7bca737f1b78ad9e3bdaffe41cc548 # scx_maestro: Make enqueue + CPU kick more reliable
8f45d373993ee2ee720755f428e9d3e55087d171 # scx_maestro: Enforce preemption each tick on CPU contention
31f08523a03f6c97903d981219682cf58961a942 # scx_maestro: Temporarily drop sub-sched support
ce985e92421b04da018ab92b020d3eed84f87c43 # scx_maestro: bump to 1.1.1
1ffb7cc6f8dc82c26e19770fa442a0c0315acb86 # scx_maestro: fix formatting
b7cb56d7d2a13128b353825f262e3061233d6a7b # scx_maestro: bump to 1.1.2
664fc16a6fd2262c7443f9da8fdc8f9eca444106 # scx_maestro: Use scx_bpf_task_set_slice() setter
5524b36b1355d44d2025ba672f3ee5b6c308dd95 # scx_pandemonium: bump to 5.17.0
07081b26abcf9b744398136724a9cf1e878cd352 # scx_pandemonium: bump to 5.17.1
ee1f3bc96faef2685b90935980280a17b1652f06 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
