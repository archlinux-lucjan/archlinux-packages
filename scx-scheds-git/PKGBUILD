# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.2.r107.g558aa0986
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
f7fa6ebbb4b9937c4a5bbdd100931a3834ff952c # scx_cake: 1.2.0 — clean-slate EEVDF-parity rewrite
0a046a36b2f2e576cdd9f097c19842be3318d68e # scx_cake: identity-gated preempt fast paths + RT-owned CPU avoidance
182258ad6dc1224ddd3c2e10141ddd9eab5d4d18 # scx_cake 1.2.0: K+L+M+S1d checkpoint — game gate passed, sealed benchmark wins
1d3b6261418be77f7e03c6a61a8b011f10fbe026 # scx_cake: G20-G26 campaign checkpoint -- first frame win over native EEVDF
9548c95a4b288471dc840874e349a60d73b5aa49 # scheds: Introduce scx_maestro
7b2149af1d63622ab35645d7a0839ca9afa6e042 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
3ae4ac4eaaac5de3aa97826ec6fb9d6eae7c7580 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
2d07a6dbb81ede3a5755726c700dffc5d476633c # scx_maestro: Don't update deadline in case of re-enqueue
985bac4784e059be44e5333931f1c2bd0d59b857 # scx_maestro: Introduce completely fair scheduling
67682244d1621a308118ace21835a1adff6c2cb3 # scx_maestro: Make enqueue + CPU kick more reliable
55322af89b364f763873985509279066c07d9c63 # scx_maestro: Enforce preemption each tick on CPU contention
52ae4150bd110a0f6e0bfc41292c643f4abd9454 # scx_maestro: Temporarily drop sub-sched support
3650c0817459a707af6edb515150fc14c6ac94fb # scx_maestro: bump to 1.1.1
ab983d5af6457dfae0fdfd2adb43aafcf5d6c82a # scx_maestro: fix formatting
c6ff94a763e16729f35cc433e01f349142d57e94 # scx_maestro: bump to 1.1.2
988f265ec259920b221c9f1f966a4039d57f8a59 # scx_maestro: Use scx_bpf_task_set_slice() setter
774714b029e885b5f566478fda4f0f4d07d90807 # scx_pandemonium: bump to 5.17.0
62d6d091206a38d74d2013d58e4b8e1f43ed1f6c # scx_pandemonium: bump to 5.17.1
5ebada66bd23a8485d03a9ae0c85b1c2bb95b3f8 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
