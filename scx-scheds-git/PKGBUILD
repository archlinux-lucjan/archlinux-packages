# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r34.g6930c8906
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
60bbc120984ad6d2d2c4d405485f2645e4f7ad40 # scheds: Introduce scx_maestro
c73f90dd7efae6296b669a88d98d0196b612e385 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
6c66f4d2d7af6bcf5a98ab80565ee262d79538fe # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
37ab4417861de6bfa8d451342664b202e01c20ea # scx_maestro: Don't update deadline in case of re-enqueue
7899f8927cb42803f36dafdd7604ad9c87950060 # scx_maestro: Introduce completely fair scheduling
f8f2654199727d75ddc6dc6f6625c5c9aafd61be # scx_maestro: Make enqueue + CPU kick more reliable
54efccc5e1e56d0458e175b802c4e6dcb118bc82 # scx_maestro: Enforce preemption each tick on CPU contention
5ba2b5f76b3ef2217d02b10d88502369fe814005 # scx_maestro: Temporarily drop sub-sched support
e02d2471217054ad9428c5904e679715574db1ed # scx_maestro: bump to 1.1.1
5ca92394b499c6d2277d17cf3a781332179ee439 # scx_maestro: fix formatting
847dbbafe972b1109309bc29cb69929aa29f83ed # scx_maestro: bump to 1.1.2
311d0a4fd32b000b50dc7ecc761c73dcd178c944 # scx_maestro: Use scx_bpf_task_set_slice() setter
43d7eb2079852be85d477604aea307e96a46eeaf # scx_maestro: bump to 1.1.3
90f72fbba20e954609993e6570a71fd7ff7c7b33 # scx_cake: register §G37 — adaptive switch-cost floor for cake_handoff_max_ns
0e5fc39c3ab36202f7cd40b42b5268f22baedf6c # scx_cake: score KovaaKs wake+IRQ A/B — sink handling wins, task-graph tail loses
a2f7d8c1831b06bbf69c3f5c7554ad7f99e45c63 # scx_cake: split the starvation predicate — relocation asks for a full turn
f7089d02dbf9a5b77d364c6e574a9b532e8ae16d # scx_cake: §G38 — rank a fully idle core above a cache-warm thread
6ae68baf75a16696e5a33077e21ef6a8344d3e29 # scx_cake: §G38.1 — core preference at the raw-pick sites + serial arm
eeca08b9c3bc2423531ed1c42d3d2e2bc63698c9 # scx_cake: §G39 — WAKE_SYNC joins the serial-handoff arm
1652bfdecf504ebc38e461683919466ca2811149 # scx_cake: §G40 — kick the idle home
2487fa3aae9e75773826279be33ea7fdee3c81d8 # scx_cake: §G38.1 amendment — the serial arm sheds its core veto
ceb9b716fa701b2934ddece962aa5da88f969188 # scx_cake: §G39-B ABORTED — pipe pays the §R.6 weld, SYNC leaves the serial arm
ba9e82cb68a0b576bf999c1f32d4829ae729b060 # scx_cake: STATE.md — G38 scored, domain audit, G38.1 fire+repair, G39 abort, G40; ledger + registry current
5a69e211e94e2845facf5166974dd503c3fb1738 # scx_cake: STATE.md — G39 revert confirmed (pipe weld removed), mutex residual is regime not build
cc91fc23d730c61d4fdf58faf3a76dbc2bd1dbfe # scx_cake: STATE.md — EOD pickup block; cause-B front-end signature confirmed (chain3, preliminary)
207e69847652441c188863f1a4e1de2451e2e2b6 # scx_cake: §G41 wake-queue occupancy mark — dispatch's global peek gated behind one word
51742202a0e943322a1a83bc09c70241356ebc8f # scx_cake: STATE.md — §G41 screen PASSED (+48.2 vs baseline +41.3, diagnostic); §G42 filed (vtime mis-charge stall, attributed away from G41)
c4cbfeac7547ee985e3e1b03a4fdcf247d239b38 # scx_cake: §G42 fix — run-slot indexing by the task's CPU, never the callback's
4852974932fc482b0a2e57a5a96db6da7f9d2bd6 # scx_cake: capture_preflight — assets root moved to scx_cake_bench
83f5a297ba83ab8881a3ce940ce89edeaeed2f0d # scx_cake: STATE.md — §G43 registered (going-idle hint claim); predict-verify audit
92f769e94beebd5490d9f64797a75c26e9dd5a8c # scx_cake: §G43 going-idle hint — dispatch publishes, wake path claims before the scan
6027c1236b81210990afb7fe1c6d37de3338ac05 # scx_cake: toggle campaign infra — rodata on/off arms; §G43 gains its gate
aca97345ef21e86076fc379b913abed7632fc6cf # scx_cake: §G44 — qmask answers wake-routing emptiness behind --toggle g44
2bbea89abf4095057656c1a4ed4014366b229fc6 # scx_cake: §G45 — event-complete idle census behind --toggle g45
b5f0d47d3aaf2c3f92ddfb2a0fbb0860810e525d # scx_cake: §G46 — departing-slice cache behind --toggle g46
99c0ee2e0f4e180ffc2291f5960f6273e8a8f270 # scx_cake: STATE.md — toggle campaign screened; §G46 the one separated result
06226c7af305225d3c762dc4db0735e47b8371bb # scx_cake: STATE.md — stack2 isolation: §G45 contributes inside the stack
ef9c6e1c3f8bb93b2d327b14bef0bf95bde0e2a2 # scx_cake: STATE.md — HD2 game screen (wake tier): stack non-regressing, leans better
00e9d3527e5f3759bd725e825adb235433058e57 # scx_cake: STATE.md — HD2 IRQ-origin service: input 2.4x tighter, stack best on GPU-origin game wakes
e2c0443587e94e0ac08e70abc32c0aad2eba07d9 # scx_cake: STATE.md — LOTR light-regime screen: stack best IPC, migration premium gone
4e36bae2393ee440c650a55b5e380cc57c699ef6 # scx_cake: STATE.md — IRQ jitter: handler durations arm-identical; stack tightest nvidia cadence (LOTR)
b13d9f7bb2194281aa3dd8512998d15488bbd649 # scx_cake: STATE.md — LOTR n=8: migration finding near-separated, stack cadence lead holds, IPC tip-vs-stack demoted to tie
dc9c3493fa23f0820757c24aaa9ced25bc1df89f # scx_cake: STATE.md — KovaaKs frame read: g43 trade mapped (menu regime favors hint on; gameplay favors off)
735ddf2e06dd0de0431b55b0df679b632314d70f # scx_cake: §G47 — ISR-successor kthread keeps the IRQ CPU, behind --toggle g47
d1d9ff4108d25579c1fd1179b7d27d870e1316b9 # scx_cake: component cost audit vs EEVDF — §G48/§G49 registered, §G47 marked built
8b2be5b799f2c79d6f63bd96b8b9d8d113b85044 # scx_cake: select_cpu redesign — self-park mailboxes become default (2026-08-23)
9010e0c2d7898a2387062f7cc5be8386e413ee8c # Cargo: update dependencies to latest versions
a7a061300c261b104e0f0037026ff6e3409c208d # Cargo: update libbpf to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
