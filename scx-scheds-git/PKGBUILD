# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r354.gd1aacb98
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
a9d195e836aae35dd50058e7475e873957b21083 # scx_cake: Sync Frontier optimizations onto latest upstream main
53f4a0a36d946aee7a8298e8911593e78fee9f4b # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
163eb0d17bb1c27afb3793ecf021e80e53a0e057 # Revert out-of-scope Cargo.toml change
b1c9d8c0c040899e40669c3669b0fe7b245f9a14 # bpf: scx_cake: Implement BPF-native clock cache
ca2c953c7d12d1ddfdfa8dfac95278725f75bb9c # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
b92aa0e3a33d82d934140e0152c888ec6142a23f # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
a5b68d88037384812c852760bd0c3165ada8f96f # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
35246c0b835588e4a60af7d833e6c1699da34e05 # scx_cake: ALPHADEV Phase 4 & Phase 9 Architectural Flattening
d42f84c8a87de35affe495239564baf64397bbc5 # Additive Fairness Model & Holistic Efficiency Overhaul
c0e358a896b1088a403c254d6357225a829daaab # chore: replace memory init division with explicit bitshift
cc38a9e4bfce9d135ef0a29956ae033e03eeb4b0 # chore: Update .gitignore for telemetry logs and streamline scheduler build
12a4fd2bdb21b30428d29f8637a4f5ddcff88a3a # chore: exclude .codex from tracking
e29e5cba3bf47e104501cf4a78599df1b398fc6a # chore: exclude perf.data from tracking
32e20806897e413da47506bddf69d09dbd1f3c0a # scx_cake: prepare 1.1.1 nightly
7aea20f3037b0ed99fe07c3673003695bde49669 # scx_cake: implement deadline-gated wakeup preemption and update documentation
54ba6e904e3cf72c364a117eea9da78181ceaa1d # scx_cake: trim hot path instruction count
94093df6bd21237324257907a34503b3dac950fc # scheds: Introduce scx_maestro
172305ca0f0277665d6fc60eaf191e5107e73ab4 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
6bbf3e513a67758ca6c34041f0c7b3e39c4a74a0 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
ca1fd43a28cfe5d29239055a8dfcc1bf94ab69c2 # scx_maestro: Don't update deadline in case of re-enqueue
701682b446b2e1da95e3150f4bbd050de8a3b3de # scx_maestro: Introduce completely fair scheduling
3095aa2d5593eca9112a85353bc18f3f39fee506 # scx_maestro: Make enqueue + CPU kick more reliable
ba763aa5484e0d98fe3f83ca69e974d2e9fa6c45 # Cargo: fix formatting
66f422c7ba64a58e5d25503bdb0f8f45fbc59121 # scx_maestro: add --completions flag for shell completion generation
c7a1345f0ff610674fb8f9d413bbcf32306aa3d4 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
