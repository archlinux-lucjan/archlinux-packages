# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r97.gfb606eaa0
pkgrel=3
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
3b5b60c9312079556f085bc39995f99c5ca5ef94 # scheds: Introduce scx_maestro
d077b4fe8a9a0846cf2adbf24684fb82fccda71b # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
c7c039df146fcd1d306bcdc795dcd208edf1dcc8 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
be2a791fc7e1a2fcbf3d3dab0edadb2db1f19daa # scx_maestro: Don't update deadline in case of re-enqueue
7b39ac544f90495b4f05b9853ae5df2ae0dd8ebd # scx_maestro: Introduce completely fair scheduling
7cd12073256eaaff458c4f0748847c1b81ae208c # scx_maestro: Make enqueue + CPU kick more reliable
1e63ab024f5df5e0b311f2edf39cf478f720e258 # scx_maestro: Enforce preemption each tick on CPU contention
ae5ddf10426e36724e86df388a4d126884ddf3ee # scx_maestro: Temporarily drop sub-sched support
068ed6b98d5de21dcf0d72f26483f868f1c36c18 # scx_maestro: bump to 1.1.1
002b4e69eeaf9e05673cd282ccac636b5175d87c # scx_maestro: fix formatting
93b88d16f0efc0e8799a8f523cb1758ca7c8d301 # scx_maestro: bump to 1.1.2
3eaacafab29c43817ce61403f40fee27ebba69f7 # scx_maestro: Use scx_bpf_task_set_slice() setter
0f6b5b8e3067b4c7e741d7ff1ff7665a79938d0e # scx_maestro: bump to 1.1.3
7183c627750a6685e1bf04d5884a21e99cd29ed4 # scx_maestro: bump libbpf to 0.27.0
a88da705e18d9e3de3a71ba9c422b0a48ed2b0dc # scx_mlfq: bump to 1.3.5
a0469c4dbb115bb07c2922daf60ea891eb889a98 # scx_mlfq: bump to 1.3.10
fb38c85b8a886480ac25f7514a34a15cee2095be # scx_mlfq: bump to 1.3.11
21ea304535ddd03da553fe4cdd9501dab1d9c580 # scx_pandemonium: bump to 5.20.0
b29856b97c2e0824cfbb51a9bac5061d4df26d33 # scx_cake: STATE.md — nightly reset onto upstream/main after PR #3767 merge
9406d4bfb805f7f8a5754e3f0447d22bc7d63c5a # scx_cake: census walk — affinity by word, no kfunc in the walk
e2d8d76a4d2729cce8f9c1e9ecf5153e92ddad91 # PROBE: per-arm placement census for select_cpu (revert before scoring)
e676f0bf0c883cf63e9b127a46f549594b0a3af8 # PROBE: cake_wake_preempt reject-gate census (revert before scoring)
ce4ce954d65689599351c1f2008861b9b2305376 # scx_cake: §G39-B' home-notify preempt behind --toggle g39b
baa7eb71c277b22ccccc09969985b18d1d59439b # scx_cake: §G39-B' it2 — continuation-arm preempt (census: starved_turn false)
e0a7ceda561aed4273859fc31591d2d94ec60291 # scx_cake: STATE.md — FFXI §G39-B' pickup, census + menu A/B
5818202a3cf6c8ae2e17b7ff289460f41f737b59 # scx_cake: §G56 FOLD — banded steal behind --toggle g56
7db0faa1400028b5e0e451c697fd588c14c430bc # scx_cake: §G57/§G58/§G59 — published-state constructs behind toggles
884d728451c71aa36877a57f0e6fce56681fa66f # scx_cake: STATE.md — §G57/§G58/§G59 wallclock fast ABBA (g57 messaging −8.7%, g58 inconclusive, g59 null)
443a1371132fa42f7dbc913af1bb577a386cbe75 # scx_cake: velocity session 2026-09-02 — §G60-§G82; the winning stack (g65 g69 g71 g75 g77 g79 g81 + unconditional g76 g80) is the default and beats crate 1.1.3 on the tail at the KovaaKs menu; experiments stay behind --toggle
f0227dd43b4ee14e0078fddf509271bcc584c258 # scheds/include: Introduce compat cid kfuncs
200c86a2100df3446f3b85861dacebcc5b9f02c8 # scheds/experimental: Introduce scx_cidland
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
