# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r349.g8e1cc4bd
pkgrel=3
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
e258bfb2dc9518e1cffcd7e87d8c5daa2fe7d943 # scx_cake: Sync Frontier optimizations onto latest upstream main
b3b2344c82a8062e201c2bc1537764d52be83e33 # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
c440efe1c36d1bb5be66e391e091b0b0eb3b4c72 # Revert out-of-scope Cargo.toml change
35ad0cb8faeaea1fb7c6c67d9c22162881013408 # bpf: scx_cake: Implement BPF-native clock cache
b51774f62e940ffadbeef7b03bb9a91b429c0f45 # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
1a809924419b3304232a006b89f535ae7c0971d0 # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
bcc780efd03a5497adace6c438eb5674900027a1 # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
41b00382b0f31b780ea686876acd104795a9e6eb # scx_cake: ALPHADEV Phase 4 & Phase 9 Architectural Flattening
dab79c37697dd62a1c9efe4f9b2a3fbb020c0c33 # Additive Fairness Model & Holistic Efficiency Overhaul
201276db8f6342717cddaa5ee978536adf70a9ab # chore: replace memory init division with explicit bitshift
8c7a7f18e156cd082d56688ee109aa3f4456d679 # chore: Update .gitignore for telemetry logs and streamline scheduler build
eacc57e28ec6fb6c5442e4eafff4fef23b203118 # chore: exclude .codex from tracking
6af151f19b3eefaa95c2716ab568c9b7580fe73b # chore: exclude perf.data from tracking
e1e2d8e13c9b70d3437fe7c770b574c2dff93b43 # scx_cake: prepare 1.1.1 nightly
a2739295ae427fc7698e44d1ae184d5025f85510 # scx_cake: implement deadline-gated wakeup preemption and update documentation
1f5630a924ff0b7cf7d62d46dc23eb2b56998b36 # scheds: Introduce scx_maestro
0a775bed89dfd38baf9ae645d1beb5998054db57 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
1b4e4462eee83424b77c44167c9aa121b20a9d04 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
55a1177e77b9cdb76bc281e289af30a676a04202 # scx_maestro: Don't update deadline in case of re-enqueue
188c48b4ad392a9b0cd3fe9117d827c47b9907c3 # scx_maestro: Introduce completely fair scheduling
446caa69875a0f5b1f12ba4dabf84889d963c093 # scx_maestro: Make enqueue + CPU kick more reliable
9aeee6a9a762af4c5b31f08646c3b202b936103f # Cargo: fix formatting
35064dc18fce85fcc2fadbf25d816f3cae15be72 # scx_maestro: add --completions flag for shell completion generation
5d502de9f293d0b048ba41c236db684a747fb436 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
