# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r340.g36cadac5
pkgrel=5
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
22ea67ee0d894cafc3c3e3258f0e6277687523bf # scx_cake: Sync Frontier optimizations onto latest upstream main
95932fd600dd67e5c7b6b5739f7084180b3809e7 # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
ae832f508833af69d6045ded5279cb9664644aa1 # Revert out-of-scope Cargo.toml change
5549bdf9213d5ca040289a8c6df9cd571c74d25e # bpf: scx_cake: Implement BPF-native clock cache
a5edc5ca748f7d9ffb43e2a2db9ea10088eba8ba # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
8aaad6bbb117d089d6f8c62feed37d4bae7cfde0 # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
0cf87bcae63ff3f2e68208d004f02e629578e1b0 # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
6817b34458ebbc9f8726949fbefc65aba72a1421 # scx_cake: ALPHADEV Phase 4 & Phase 9 Architectural Flattening
dd6332da2aba560be4e2b1a2a2ff0426986445da # Additive Fairness Model & Holistic Efficiency Overhaul
cb72b5f9c56ca789e841d04c64b96e4a7af1701c # chore: replace memory init division with explicit bitshift
5274dbe1f860f8e637f39a20ce4f1f3b2e3ff3cc # chore: Update .gitignore for telemetry logs and streamline scheduler build
efb591b5a8ed77d393545e2d37d508192000cc5a # chore: exclude .codex from tracking
d5f15913d9e662e0040b525db63023f7975cf3fe # chore: exclude perf.data from tracking
5d2759221174d3857270d7198799239633cd1c63 # scx_pandemonium: update to v5.6.0
34172c0c3e123eb5c9cdfa0eb2937f3172d7594a # scx_pandemonium: fix verifier bounds for older kernels, restore veristat baseline
9aa82f903aeb143a741c600d14d8c7dacb1766cf # scx_pandemonium: fix interactive starvation rescue, verifier bounds
0169fa13277c316cbc54964327f35a7d2df5dac2 # scheds/experimental: add scx_flow v2.2.0 scheduler
6e7d6810fcba180d625a6019ff6772251e1c2541 # fix: update Cargo.lock for scx_flow v2.2.0
395c02e153155e2287634d8ca87fa500c52f9328 # scheds: Introduce scx_maestro
857d990f7a31db72fdb816980536cb70cb7593de # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
a23b66eac716f69662195b140c34abe4d9cf27ce # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
8f7310d10abbd29969b5ff5f965b200260770040 # scx_maestro: Don't update deadline in case of re-enqueue
ca54c84325511353604ab71950a1bad50393e11d # scx_maestro: Introduce completely fair scheduling
bd636ff54841cc6d6372653f86e4451007eccb50 # scx_maestro: Make enqueue + CPU kick more reliable
f3e94640f4e4a5363a00006b49b98b6f8b5a5019 # Cargo: fix formatting
9165fabe2d0f742457b801334d0126c8ffe26bae # scx_maestro: add --completions flag for shell completion generation
e70d1916deb58aaf57418f14415e5f1f0240832e # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
