# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r340.g36cadac5
pkgrel=4
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
22ea67ee0d894cafc3c3e3258f0e6277687523bf # scx_cake: Sync Frontier optimizations onto latest upstream main
95932fd600dd67e5c7b6b5739f7084180b3809e7 # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
ae832f508833af69d6045ded5279cb9664644aa1 # Revert out-of-scope Cargo.toml change
5549bdf9213d5ca040289a8c6df9cd571c74d25e # bpf: scx_cake: Implement BPF-native clock cache
a5edc5ca748f7d9ffb43e2a2db9ea10088eba8ba # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
8aaad6bbb117d089d6f8c62feed37d4bae7cfde0 # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
0cf87bcae63ff3f2e68208d004f02e629578e1b0 # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
6817b34458ebbc9f8726949fbefc65aba72a1421 # scx_cake: ALPHADEV Phase 4 & Phase 9 Architectural Flattening
ccdda1cbcfedcd813f0dc40f5c31853eb4433233 # scx_pandemonium: update to v5.6.0
3dc242b140e8d338a709398f2e8e112ade825013 # scx_pandemonium: fix verifier bounds for older kernels, restore veristat baseline
bfbe74a66ecab2a75779615db1a4b4e28678fc8b # scx_pandemonium: fix interactive starvation rescue, verifier bounds
36047fa774496df4a8ea4f07a3dc94626733ca2f # scheds/experimental: add scx_flow v2.2.0 scheduler
318aec1355cfccbfbfb004a830cb27f85a40621f # fix: update Cargo.lock for scx_flow v2.2.0
d3365a38cef39c0a905267572ee9a8e86f82075b # scheds: Introduce scx_maestro
1c6b57a7a2fcbb9b7b5313db75be31db313efe50 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
e706290726d2cf3eee59f9f48e706c357394c5a4 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
65d45cb65bc8312f491d5fa4bb2706d722202372 # scx_maestro: Don't update deadline in case of re-enqueue
c022938a875ce423761124edf85a74d52d909cf0 # scx_maestro: Introduce completely fair scheduling
76f3c8aefb59ea2b62bf48f510065dfe5b0db878 # scx_maestro: Make enqueue + CPU kick more reliable
40994f497689b2c7fe294dde11d18e014d128ac8 # Cargo: fix formatting
afdea7736daf781abe612f6555f7c006dc04fee2 # scx_maestro: add --completions flag for shell completion generation
87e2898100be4a88b3ce6649802483547eb203db # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
