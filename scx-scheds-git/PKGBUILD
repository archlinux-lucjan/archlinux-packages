# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r399.g92785ef7f
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
e5a0a78d74fde458ea26efe5cf1771bb1a0aabd1 # scheds: Introduce scx_maestro
b6c55b195d38d01f5207c08e880fff950bba90ec # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
237c7b0c6ad532f70f345e4d74ff18428284ffbd # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
7d3ee5cd6ebe2f35b41f761786253d0a84755fff # scx_maestro: Don't update deadline in case of re-enqueue
fca9f882f6f5e035617c1a49d7e852bbb32b3678 # scx_maestro: Introduce completely fair scheduling
6f1a464d3289a89e5769a45a3a529bec4dec00ef # scx_maestro: Make enqueue + CPU kick more reliable
6fcf690058585fb9981783af5fe6626bf4f2b0eb # scx_maestro: Enforce preemption each tick on CPU contention
e0b9dab3a1d03cde8e84fd39296fd8d02cb712e1 # scx_maestro: Temporarily drop sub-sched support
7c85857f42e0f544ab4126ab3d652c1664426770 # scx_maestro: bump to 1.1.1
b7cf0bf74ad89e32571d5ad7d359ceb5100a58a9 # scx_maestro: fix formatting
adbdecabf477a9db2fc22dac7ea2699ecb1fdd34 # scx_maestro: bump to 1.1.2
9e60b3c12ddfe8d87a77ae7fe73b95fcd600b116 # scx_maestro: Use scx_bpf_task_set_slice() setter
9bebc6f8be102c78a0b760ecb026df94b018bf87 # scx_maestro: bump to 1.1.3
49d67b7164c2ed63c9a81c06603350b88ff125a6 # scx_maestro: bump libbpf to 0.27.0
6c08bed0b7bc9665f92b7a55837770627e2e7d37 # scx_maestro: migrate to Rust edition 2024 and rust-version 1.91
e53c62b23d463814c301e4c357187e352e03692d # scx_maestro: fix formatting
77f0ee648b76560e15d499f719b33c3b0f37e3b0 # scx_maestro: bump libbpf to 0.27.1
6ce1f53d5b08b6db632693fa5fd71687dc7b9977 # scx_cake: consolidate nightly scheduler updates for Rust 2024
95972939e5772cc33e94a58342adfd0c4f55126e # scx_cake: publish IRQ sink words with a Relaxed atomic store; is_multiple_of in the hop probe
05fc8ad14edb3af44f5bdd96b80ee8bd250997ee # scx_cake: dispatch peeks a head only when it competes with another; one DSQ hash lookup fewer per own-only and pool-only dispatch
251a00686260b9811eab61622accaf6dc9411afe # scx_cake: rq clock for the stamp family (run start, occupant, handoff, pool served, probe); ktime stays for start_time and tick deltas (§G34 H1)
33d6e69442a712b6240e1674f7e1e9f5308c31c2 # scx_cake: running reads task storage only while some seat is held; one lookup fewer per switch with no sleeping stage holder
0378201e68aeadac9c397559a1255acf20e963c6 # scx_cake: a lone continuation kicks no idle CPU; kick_alone census; census name table catches up (pool_direct)
a83ffb1bc12af780230f92bdc151309ff4ef7a5d # scx_cake docs: operation cost ladder + site map audit (2026-09-15); completed reviews to archive/; TOOLING tracked; index reflects the vault as found
8bcdee19c5472e22344f3b1979244e953cefa01c # scx_cake: hot-path efficiency: select_cpu 118-130 -> 87-90 ns per wake on live WoW, scheduling unchanged
f80cbf281c2d87a5b70a569b64f3868f734f92cd # scx_cake: notify always searches for an idle CPU; the empty-word return uses the un-masked word when g89 is off (review 2026-09-17)
a2d1ab5e83bcbfd5f59a8fc78b81f39fe201c03b # scx_cake: 2026-09-18/19 nightly: time-gap audit and council rounds squashed (22 commits)
3dbd71ad44124d13c81d21c8d45c7fc230923109 # scx_cake: 2026-09-19 nightly: self-sizing, event-complete handoffs, pool-token proof and the instruction rounds squashed (18 commits)
fb6f6eadde21cfbd28dd23cdc985bd156f94ce93 # scx_pandemonium: bump to 5.21.0
d0461f50f76eb08e00cfc55520acad576046ca24 # scx_pandemonium: bump to 5.22.0
3567d2656738822c8a6216bd56845e645e907972 # scx_flow: bump to 4.2.41-o1
3f2e0c41a0b8bfb9ff31105e6e544b90a5b64da8 # scx_flow: bump to 4.2.46-o1
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
