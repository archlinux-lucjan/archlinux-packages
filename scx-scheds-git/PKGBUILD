# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.1.r211.ga8a638e2
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
fbffa02106947a4702738f50c3a91a63bdc586b3 # scx_cake 1.2.0: hybrid-queue champion (LLC-vtime + hybrid4 game-tuned defaults)
139a8263e7bb63a49283ce44317be8dea986f8d1 # scx_cake: fix watchdog stall at source — drop llc_pending bitmap (Option A), remove rescue bucket
b2f7e3e36af6321337f0eac24ba9a94992336047 # scx_cake: silence build warnings + cargo fmt/clippy (no functional change)
64cab39b60961ec04a963839214d2ef9d9ab5de5 # scx_cake: remove global core_steal_pending_mask bucket (per-CPU truth)
0f0dc5e47371e27d12d368c2f790e380ecb6109a # scx_cake: silence debug-build -Wunused-function on config-gated helpers
69c16ef9d4af5b9fb1ab5f1b9cb3c2f1a41d63b8 # scx_cake: promote vtime-local nightly candidate
600e88666cabfe635826f696367574551f8d1e34 # scx_cake: unify local-waiter wake (preempt-kick) + free memcpy slice; gated service-readiness instrument
c4f9e3914c77e5366f08c5a122a403e19a0c18f9 # scx_cake: dedup shared-escape twins (unification, behavior-preserving)
ff636330a19b4891fd806ff90cbbd96674862ec7 # scx_cake: EEVDF-like wake balancing for throughput class in vtime-local select_cpu
94730d16b3718b8fdba6f5bb05a8f98f3cbca6f4 # scx_cake: drop redundant self-kick on local non-preempt wake
0b819d37976c0e8f7ab467e2dc85d67d550fb1f0 # scx_cake: rebalance throughput class off congested prev (EEVDF wake_affine)
1d3750668ea1f32b193e598700eb7f2c49e7b0e8 # scx_cake: integrate SMT-aligned zero-cache-miss migrations and Arena-free hotpath
7f71db659bb25809af5906fd3a67357bbb0a4096 # scx_cake: fix build warnings and gate telemetry functions in release
39cc7df86288277a1a90766eebed43cdce54220f # scx_cake: fix Cargo.lock
6daf86a1f3351e0a6885e8bf794d9360958609f3 # scheds: Introduce scx_maestro
72946acfdfa9a64565edb3c1fe41d90cb4e060f4 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
93123cbb134c054586ca265b20b9a595e304df05 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
614d17d0f1818bfd2642045e0a9ca0b11645457c # scx_maestro: Don't update deadline in case of re-enqueue
ef024898c85875116c5d1d843145f072a438a359 # scx_maestro: Introduce completely fair scheduling
58d38bdac91df7aadcf9cb215d75158d50c61b0a # scx_maestro: Make enqueue + CPU kick more reliable
9c1f86206dfd09ff2b5a0dc01153f2a3937c00c0 # scx_maestro: Enforce preemption each tick on CPU contention
0641f6f5a1c7475b94588e140bd23f466c9760c2 # scx_maestro: Temporarily drop sub-sched support
13b1b0b72e126bd9a9616fb0831c54d764edc605 # scx_maestro: bump to 1.1.1
e70240ff1b1e3542e6c9f2ae659f4f92c71f4cdd # scx_maestro: fix formatting
fa4a74f8eafd5bf1c77a63e97b81465484e03035 # Cargo: update dependencies to latest versions
9314a6c44fa1a197ddd375f698a87e1173497aba # scx_pandemonium: bump to 5.16.0
6e73cb30b6c24c518448583e4b3bb233d6b28135 # scx_soft_domain: ‌through topology-aware scheduling to improve performance
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
