# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.2.r70.g736e9b2df
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
1b7f30106afd557efeb29929131af8065efe9061 # scx_cake: 1.2.0 — clean-slate EEVDF-parity rewrite
f847da35b66666b78d6ad53f2d5c77b90319220c # scx_cake: identity-gated preempt fast paths + RT-owned CPU avoidance
3b7713245cae2758029ab0f0be75a678704ad459 # scx_cake 1.2.0: K+L+M+S1d checkpoint — game gate passed, sealed benchmark wins
391729285f63b4703454d08a2a2309923d24c057 # scheds: Introduce scx_maestro
9f6f3ab0ba0705b7d2e06dcf099764103e13e84d # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
5fe1e98d2ed391da79fc3d31b021656af47826cb # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
52a121808a0f072d35ff54711f7fb24bf6795f56 # scx_maestro: Don't update deadline in case of re-enqueue
7c10155dc5d436fe8f17f294173c832b30304652 # scx_maestro: Introduce completely fair scheduling
f406675c72a4ebecc2715519a9c285fef09cd3aa # scx_maestro: Make enqueue + CPU kick more reliable
351af3013f813dbe823a15918919f46edd54b354 # scx_maestro: Enforce preemption each tick on CPU contention
5b28a0ae1e5e1dab11c3183016a9ec288685c0dd # scx_maestro: Temporarily drop sub-sched support
5de92214a557353e986a9aa8700a083a24e30902 # scx_maestro: bump to 1.1.1
af624265e37700827d0f14278b0248fca8ec3baf # scx_maestro: fix formatting
0ad68092f4e97d026d2bc14864bc24d827312734 # scx_maestro: bump to 1.1.2
88f7093bde86a5a88bba9815582bef89589c5dd0 # scx_maestro: Use scx_bpf_task_set_slice() setter
7cefda7de3b0441546647f1047cb3c1f33742cd8 # scx_pandemonium: bump to 5.17.0
b8578c1d065db68a82cb347ebdbcab59aaeb67b1 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
