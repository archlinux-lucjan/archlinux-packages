# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r57.g8bd309a17
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
bcddb52e4da8f133d083760d0fa6f4d702794267 # scheds: Introduce scx_maestro
3f2b5f44bd465c3902f064c7e06c32c027e0f3be # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
7106f6cf9470675e8fb1a77481dd28078afc8804 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
cf3167f1340f17721d20d8c3e9a49d0f84099ee1 # scx_maestro: Don't update deadline in case of re-enqueue
9a881641ee0aab00b36ec50a37849b04aae2ab7b # scx_maestro: Introduce completely fair scheduling
552f46de791f048f932bbc236d3ff9653ec840b6 # scx_maestro: Make enqueue + CPU kick more reliable
b3bb687455f7d43f7b67517ceda27890485d061c # scx_maestro: Enforce preemption each tick on CPU contention
575efc4a44d8a238ce4531edf9a33c8f23aa6f10 # scx_maestro: Temporarily drop sub-sched support
990335a4a60aafb86b7089f375ed4344818ef18f # scx_maestro: bump to 1.1.1
2c26e4a562ea292d5790174b1f7c6bd885d02496 # scx_maestro: fix formatting
cf1ee4c5c11db4b3a061e6b69d1c1bf55e69a4d6 # scx_maestro: bump to 1.1.2
deb77cabba40db83a53da9c9d0c671eb4ae33e58 # scx_maestro: Use scx_bpf_task_set_slice() setter
cdeec4b0544aea7574f12bc993f30671c669c8a9 # scx_maestro: bump to 1.1.3
0825e0460d747adccf102d025bd9f5fc0dd11017 # scx_mlfq: bump to 1.3.5
584823e9dac86f434c7dc3c9d43ecd7fdb28af80 # scx_mlfq: bump to 1.3.10
bb3261def65edd55570264d4aded92fb55db45bc # scx_mlfq: bump to 1.3.11
e1d279a61a8d78c52ed3e7af45dafe0a8fafeb62 # Cargo: update dependencies to latest versions
b795c1ea61f28acd6ba30361957ff1ae5cf52549 # Cargo: update libbpf to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
