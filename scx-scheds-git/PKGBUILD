# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r358.g3c4506bfd
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
ddc443fef4188a48c6f0277df0ec22be49cf50b5 # scheds: Introduce scx_maestro
2b8af4982efe1872e119874abaab4a538d470148 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
ab2357b385edac1ea2cbc0fa757c7c6c90b15a41 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
d6d5c0846711129c494a1815c77b6dc4a961d66a # scx_maestro: Don't update deadline in case of re-enqueue
b6c2894c3ddb5d8cd8be25c2545dd9942926b1f1 # scx_maestro: Introduce completely fair scheduling
aa84835c8d87ffbdbde34c056d68a3611217972c # scx_maestro: Make enqueue + CPU kick more reliable
9d2a8a84923dc95ebe9c8e9315bfad21b315e14f # scx_maestro: Enforce preemption each tick on CPU contention
f190032345c26994e1c6c09d58e47ae59c687525 # scx_maestro: Temporarily drop sub-sched support
1fa7646460b6059bd7d2f1aa7741fa0e3923d882 # scx_maestro: bump to 1.1.1
3a441d0ffb411d8db9f9c3964692c0649c2efced # scx_maestro: fix formatting
0138cc9744015bdbe233e5d45b7a45f6f597fc21 # scx_maestro: bump to 1.1.2
19db21e9fd849e1fd6e02bee82ae2f731116240f # scx_maestro: Use scx_bpf_task_set_slice() setter
8973fc4e70e02d5ce022bf33fe366aff11a4bf05 # scx_maestro: bump to 1.1.3
c2b1bb8abd9d44e3903602e47304f541d3da5f90 # scx_maestro: bump libbpf to 0.27.0
8cae9f54a7fac1ccde41365d55ec913da931d020 # scx_maestro: migrate to Rust edition 2024 and rust-version 1.91
b2790d6a5e8d2ed45e0930bab1ef8e4c32b14f51 # scx_maestro: fix formatting
d322ab4070f5e84f2976bfc8f760af6cfc08f584 # scx_maestro: bump libbpf to 0.27.1
cefeae47cdbfcf565d2d589ec5e6cfaf3e4de3be # scx_cake: consolidate nightly scheduler updates for Rust 2024
5962569f8606abd1fc9768c7a8dc29186509d8b9 # scx_pandemonium: bump to 5.21.0
68108b24577f9b96ec79602aa2286d587595fb0b # scx_pandemonium: bump to 5.22.0
615bc35a33c83323c8cd5e1c010e074262212cb6 # scx_flow: bump to 4.2.41-o1
dc9bde0f37b4da62cf9861819145b1d47b349054 # scx_flow: bump to 4.2.46-o1
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
