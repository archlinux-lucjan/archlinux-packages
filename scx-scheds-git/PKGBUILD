# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.1.r196.g2088c10c
pkgrel=3
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
77ac2f587f458e620e23fda08daf83b3d54cbd51 # scx_cake 1.2.0: hybrid-queue champion (LLC-vtime + hybrid4 game-tuned defaults)
d82888d9e51305e660b5f7b0a6e42f812f0b5a4f # scx_cake: fix watchdog stall at source — drop llc_pending bitmap (Option A), remove rescue bucket
c17b8166e94025108eed3ddabbaf0de0c46387e0 # scx_cake: silence build warnings + cargo fmt/clippy (no functional change)
0113d6add97e2d255abed1e3118d7ae79415bc98 # scx_cake: remove global core_steal_pending_mask bucket (per-CPU truth)
8d4c7198bae3f81f62eaed5ae714bb80d165ca6d # scx_cake: silence debug-build -Wunused-function on config-gated helpers
fb76ed02364e2993a9b3fa2a1854b5b3c364aa40 # scx_cake: promote vtime-local nightly candidate
b45bf4425e098e64552c1cce8f24b83df72c43a4 # scx_cake: fix formatting
ca84d8e1ebd9faa505d75231fbfd06bc7ab2cc15 # scheds: Introduce scx_maestro
74d49e401cbf53f6f94bd2d2a04465cc31b91b80 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
0116b0eb45ae856ab741ef6ecfeadf9a1c77b019 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
340b1a0db8f8ac3e58fae3e4d6d198b6f73979e3 # scx_maestro: Don't update deadline in case of re-enqueue
786c2aff6981d0434f8d053a2134b7dbe83589e8 # scx_maestro: Introduce completely fair scheduling
e6205c5268c7f14f55144835a13fb7d584eb6a6e # scx_maestro: Make enqueue + CPU kick more reliable
56258efb766a5a80d9129cb1711813e3301148a4 # scx_maestro: Enforce preemption each tick on CPU contention
9a194d944cc5c04afeb09009173ad0f73196b6f5 # scx_maestro: Temporarily drop sub-sched support
31f084bcd88db6821c7b807a705e3f414909c1c5 # scx_maestro: bump to 1.1.1
e86c52204bd4bef38ff9ecda1096b9bd02d08cb2 # scx_maestro: fix formatting
b7975ea1fa2f2f367b00e435733e4809b8a44767 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
