# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r65.g3a0169475
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
481c14d07e3e2692e31eecf41fcda80fbf6c4380 # scheds: Introduce scx_maestro
138616ed4fd7e8ac7942135b1e390c7a5701d011 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
249cc08b37252ed9152bacb317c59101d4250b24 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
3decdb6fa5ac42f6bf94a8eda1a3d6aa75c419f3 # scx_maestro: Don't update deadline in case of re-enqueue
a2b5eda75798567b1be0ae15a39ae1d29801e49d # scx_maestro: Introduce completely fair scheduling
c3deb75cd66b5425c1eef19c3e26e3e473dc1000 # scx_maestro: Make enqueue + CPU kick more reliable
f7adecbc8a6d950202aa818cd6e34e1f1e0e4c12 # scx_maestro: Enforce preemption each tick on CPU contention
e0f194f5d07c118c6f519f6baa6afdd4a88e5aae # scx_maestro: Temporarily drop sub-sched support
1f50d8cdb2767c5086508675abe36a35091c2adf # scx_maestro: bump to 1.1.1
c4921c74598333880fb6f2ffd6a0ffc8f3922993 # scx_maestro: fix formatting
0cfa68d20bbbe346bc1459036cee0f5756ed6a7f # scx_maestro: bump to 1.1.2
75ea8c07a85059682d7f4b113949a7ec5ac539a2 # scx_maestro: Use scx_bpf_task_set_slice() setter
f5c744b215972fd79281be556c6bec638df7fe9f # scx_maestro: bump to 1.1.3
60d374b09af21baf0e09b7af49df3ccf4d9c4e63 # scx_mlfq: bump to 1.3.5
51810d97c09f41059b667f2e4dfadb4027c83570 # scx_mlfq: bump to 1.3.10
1432cb6380d8b85324966ccf43b7f393a3694057 # scx_mlfq: bump to 1.3.11
61edd27e7d6ab889a54671087f485aaa76017b88 # Cargo: update dependencies to latest versions
e012af544497b65407fb2349f191994e6e2e2c33 # Cargo: update libbpf to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
