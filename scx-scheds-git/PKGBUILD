# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.1.r199.g7632c961
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
f8d372ae3e28b76d94d4b978c4e974e6dec5ef24 # scx_cake 1.2.0: hybrid-queue champion (LLC-vtime + hybrid4 game-tuned defaults)
edda388a292ae40ce5ce703d7f57a14b7101108d # scx_cake: fix watchdog stall at source — drop llc_pending bitmap (Option A), remove rescue bucket
b836b96a7d394be869fadb4442ec832e33b6a0ed # scx_cake: silence build warnings + cargo fmt/clippy (no functional change)
f987204a8b95cf356afb4d6909d6f52c7d007ab8 # scx_cake: remove global core_steal_pending_mask bucket (per-CPU truth)
f204eb75ce4d813ff88074d680896ed9ecee3c6b # scx_cake: silence debug-build -Wunused-function on config-gated helpers
b35b9f0becbe1cf34fa8e323542bd1dcf6eacfb5 # scx_cake: promote vtime-local nightly candidate
3384e00242465a5f85d64c6df1ff9cd3c808b68e # scx_cake: unify local-waiter wake (preempt-kick) + free memcpy slice; gated service-readiness instrument
c8e91e3608d2d08ad81f1046e2d30b4bd58f1cd6 # scx_cake: dedup shared-escape twins (unification, behavior-preserving)
42f5d31479965b32d124e13c05330d8ca5bf0775 # scx_cake: EEVDF-like wake balancing for throughput class in vtime-local select_cpu
da85f8c30dd46c4e8f14795c81106d29d3c6858c # scheds: Introduce scx_maestro
aae68dd616a212ee3b8ed70f3da08d8fde99849e # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
de4dbda11f58b47e14faea6c0dfff42ea5ad78a7 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
dff77e24eb930115e36d8c021f9f75c871a1f6b6 # scx_maestro: Don't update deadline in case of re-enqueue
4ac5f0504bbbd8836a033f1e593771842ccb9c9b # scx_maestro: Introduce completely fair scheduling
cbfa16043fb998ec510b9a469b1230fc018361d1 # scx_maestro: Make enqueue + CPU kick more reliable
799012c5c1da263f513cec3fe1a6efb3ca8f2771 # scx_maestro: Enforce preemption each tick on CPU contention
2145cf36cb81a6b0724a1cb54fd5f2abd8f37ed3 # scx_maestro: Temporarily drop sub-sched support
3b36141322550cd87f7ff5b5ecf1aa5c3194dcce # scx_maestro: bump to 1.1.1
3f35bc783081ffad67e837eb65b32fc91ad06ddf # scx_maestro: fix formatting
7e17e500819e4ec6ea7fc50184f87ee4e0374070 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
