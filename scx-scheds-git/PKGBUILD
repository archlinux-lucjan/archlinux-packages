# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.2.r127.g1741fa2b0
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
c3953b64954deb4f2fd6a457b0d02812584a8cab # scx_cake: nightly squash — rebased onto upstream ff86d6588 (2026-08-18)
64327e9f584e160fbfc0da4bcc1d6ff71bc36fe7 # scheds: Introduce scx_maestro
b5b20411db7c739d6483ae5a1ce99b680e0f9be3 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
858ba6845ac5dcbc46dc406dd01de69ee12eb9a0 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
0a50367bafa13d290e9229c062812bb32dba5b24 # scx_maestro: Don't update deadline in case of re-enqueue
d9598be7cd5eab5ee02f51dfde34691de6e038d8 # scx_maestro: Introduce completely fair scheduling
b53a85d4e6cc81c51a803dcae59e7f68be075485 # scx_maestro: Make enqueue + CPU kick more reliable
8e6d5941c31692211fd37f3f5da93e9a8e3224ee # scx_maestro: Enforce preemption each tick on CPU contention
3d5fae69669814b566c4c77132dbbe98ed0eaa1d # scx_maestro: Temporarily drop sub-sched support
3fa098e13c0c17e09da49f1612abae4397711806 # scx_maestro: bump to 1.1.1
8555fe4afbb769cdd8b01a17f492aa712795c8fd # scx_maestro: fix formatting
33a7c4162ca391c143bb8dd3731cbab1e0b25e57 # scx_maestro: bump to 1.1.2
55b64dfe11dced37ab54e0e714c0924c84483aa5 # scx_maestro: Use scx_bpf_task_set_slice() setter
a7e0171feb12ca2164a58b30b8ed15e6675d00ff # scx_pandemonium: bump to 5.17.0
8198f5e47043e3185b6b28332ad2581720b8a374 # scx_pandemonium: bump to 5.17.1
d352ca96cfae59c656ee87728726e75c288bd304 # scx_pandemonium: bump to 5.18.0
2faf935f29f4dc38b9529fecddc1cf1cc0106813 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
