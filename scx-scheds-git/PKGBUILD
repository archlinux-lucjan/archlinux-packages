# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.2.r122.gff86d6588
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
1f3c59eb9daa5963513e83988d404861cf1fd743 # scx_cake: 1.2.0 — clean-slate EEVDF-parity rewrite
20b5d884e7a54b7f8b1c47c6c0f6cd234732ccc7 # scx_cake: identity-gated preempt fast paths + RT-owned CPU avoidance
ded1d9cffad80daf7eea02180ee1b797193f43e7 # scx_cake 1.2.0: K+L+M+S1d checkpoint — game gate passed, sealed benchmark wins
525f5a3d602e82d666533088e16d9e9543fa5831 # scx_cake: G20-G26 campaign checkpoint -- first frame win over native EEVDF
d43a142d1a42aacb031079024f6f520a52c3089a # scheds: Introduce scx_maestro
41dba42ff986dd86707cbb529d09626cf8364695 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
47364ac1389c2f0993c7608f1540cb290c5fcffb # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
3fe293a46bdfe4b5859adb1ac45a2935de629125 # scx_maestro: Don't update deadline in case of re-enqueue
aff81d4bba0a1b1d47b82e6b3891ea8a2cd8750a # scx_maestro: Introduce completely fair scheduling
309dda518307252004a7f2d99ef6a2759da8d3c9 # scx_maestro: Make enqueue + CPU kick more reliable
dec6d41ea40c9b6bd68599f04f02dfd7cb0c66e7 # scx_maestro: Enforce preemption each tick on CPU contention
856bca448cef237cff0e077e21ff80bae07989f3 # scx_maestro: Temporarily drop sub-sched support
55342f970eff31500274577c94ff7b5e487afc55 # scx_maestro: bump to 1.1.1
62d19cc3855eed0e34ff2b9fb036f85690f6c299 # scx_maestro: fix formatting
702a62e95e1d9d1c2c5a023e06a3a3e641d70199 # scx_maestro: bump to 1.1.2
07bc26aea7d554474d44f7f62903a5fba6d5f4ff # scx_maestro: Use scx_bpf_task_set_slice() setter
8f8a7c67a6fb8c436838bfdd4ec0f8017097e7ae # scx_pandemonium: bump to 5.17.0
510ec29fcf1d7e164d27938bc3ccca9ae93c7e59 # scx_pandemonium: bump to 5.17.1
029616ce39d45dc8c527695eb66d9447678d2131 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
