# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.1.r209.gb911893a
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
26561ce2c8132b756928f3068af3710f9af56316 # scx_cake 1.2.0: hybrid-queue champion (LLC-vtime + hybrid4 game-tuned defaults)
821dbaabbb5dc4394ecb0d57db05d52144f10a36 # scx_cake: fix watchdog stall at source — drop llc_pending bitmap (Option A), remove rescue bucket
56252b2ed819945831ab62a2d7fec8121ca5f32c # scx_cake: silence build warnings + cargo fmt/clippy (no functional change)
7c32a46aa17c8d24389517445cf87fb8dfaf0507 # scx_cake: remove global core_steal_pending_mask bucket (per-CPU truth)
649315494b8e54ee99081e4ece1537312cfaeb0c # scx_cake: silence debug-build -Wunused-function on config-gated helpers
718540b4be073285633bf316ec9ca2ccffd82f2c # scx_cake: promote vtime-local nightly candidate
1e294c3e17ce9b97f5e3f1d9b19c475165e9551b # scx_cake: unify local-waiter wake (preempt-kick) + free memcpy slice; gated service-readiness instrument
64785f33ed22d6845024f4866b76153dbb5e91c2 # scx_cake: dedup shared-escape twins (unification, behavior-preserving)
d4639e424194d5208ad9a4d3c403b4c5a48b3a69 # scx_cake: EEVDF-like wake balancing for throughput class in vtime-local select_cpu
e3afc8b05f61523c19991aa45163c8d3b03bafe5 # scx_cake: drop redundant self-kick on local non-preempt wake
f18907d1df2a4b8b9cd96e033a8d5e022fa7ce0a # scx_cake: rebalance throughput class off congested prev (EEVDF wake_affine)
57d1c714ee7b8817baab92ab830ff508d4cf4061 # scx_cake: integrate SMT-aligned zero-cache-miss migrations and Arena-free hotpath
ce607c3d18d2c10713b17c7dd27afae1d0cbf454 # scx_cake: fix build warnings and gate telemetry functions in release
1da43f291eaa2757dff732880794cb4f5394ff71 # scx_cake: fix Cargo.lock
e67ddc1657f3c2aa1ea1e02d13bdf340417f54e7 # scheds: Introduce scx_maestro
f1c55dcb4a6ffcd21a6b032e3efa45ee64e7cf02 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
8d7606144c5ac6c9687921ea7be0dfed0dca3e54 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
1c3151f98414e68affc93d62b495fda07b8f8516 # scx_maestro: Don't update deadline in case of re-enqueue
9e62f0368c98e19a50468a207c393bc3e10f6814 # scx_maestro: Introduce completely fair scheduling
a0a388acea94a0175e69f85bdf84633a6f64b778 # scx_maestro: Make enqueue + CPU kick more reliable
53e6682933640aadf7176a41d9ddd8821091e096 # scx_maestro: Enforce preemption each tick on CPU contention
0f63a912717978f7070cab52f0ae5204b69818cc # scx_maestro: Temporarily drop sub-sched support
fdf0b73c0c5df8821219541924f76f677eb3aec8 # scx_maestro: bump to 1.1.1
0259da84c947f88e29241d90777cde0660371e99 # scx_maestro: fix formatting
97037794917eacbd26ad284a41b9b4faa56defa8 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
