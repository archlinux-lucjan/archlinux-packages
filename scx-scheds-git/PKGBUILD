# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.1.r201.g19ed8749
pkgrel=7
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
d4c02970df09059fb3874c7ff4bc484a24207e9b # scx_cake 1.2.0: hybrid-queue champion (LLC-vtime + hybrid4 game-tuned defaults)
ff73881ca9adc8194b41837ba1e8dae4c3a700a3 # scx_cake: fix watchdog stall at source — drop llc_pending bitmap (Option A), remove rescue bucket
3ee949104ed4669435d02e739838b9448af74004 # scx_cake: silence build warnings + cargo fmt/clippy (no functional change)
d9419a1f16822c441b058ae72d2874787981c188 # scx_cake: remove global core_steal_pending_mask bucket (per-CPU truth)
f12a5ee62b759e5a88d1bb2b0c5a92d9d771a203 # scx_cake: silence debug-build -Wunused-function on config-gated helpers
29a8dcf4ff01cd0bc1388312f27329334620ffc7 # scx_cake: promote vtime-local nightly candidate
98fc7bc6aeb479281e9fb3d08f14151a441b7c23 # scx_cake: unify local-waiter wake (preempt-kick) + free memcpy slice; gated service-readiness instrument
29aea84eb7aee9b9bebeed3e8a3a834e3be40dc8 # scx_cake: dedup shared-escape twins (unification, behavior-preserving)
6707e8d64aa9f587421e3c5191bddaeae301c105 # scx_cake: EEVDF-like wake balancing for throughput class in vtime-local select_cpu
aaae2f62323e289c017807598a182c3b9f3d0015 # scx_cake: drop redundant self-kick on local non-preempt wake
d00044485faf93f0d2aa6c6c8625644ce384427e # scx_cake: rebalance throughput class off congested prev (EEVDF wake_affine)
4e6bba020a830a8980a0b1da9f72d6d9801b8043 # scx_cake: integrate SMT-aligned zero-cache-miss migrations and Arena-free hotpath
0fca974c9e4c88e031432ebd6d7a22503e7c89a9 # scx_cake: fix build warnings and gate telemetry functions in release
4aec0df4bed802fdf109ce436bd1b2dccd1d6146 # scx_cake: fix Cargo.lock
f86d6755553ced8cdf7aefd407cee71ba7eff4ae # scheds: Introduce scx_maestro
cdec20d89ffae2b647b323d6fdebd217a484f65f # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
a6a2b11b4728741108b07a5bb2d00abe52b56195 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
b20bbdcb76d41e23b3e91a40ffaa7bb76b627b9b # scx_maestro: Don't update deadline in case of re-enqueue
72dd02a04e162f09f60a325c330535224a1e39fc # scx_maestro: Introduce completely fair scheduling
f1e406d6f8adae938fcd708840510c926d31badc # scx_maestro: Make enqueue + CPU kick more reliable
528e0443b3bc1aa52261279e0dada45ed0f61a50 # scx_maestro: Enforce preemption each tick on CPU contention
6e5115add2ad7b919695f733a58381ed151ac983 # scx_maestro: Temporarily drop sub-sched support
7c9d96b1fe3a40df62fdd696ebf3213f22950671 # scx_maestro: bump to 1.1.1
bf87c8e6ddfdf0d5195767b070568fa29d327189 # scx_maestro: fix formatting
ba039be7855fa747b9b28f1fef1d2dc257e14c20 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
