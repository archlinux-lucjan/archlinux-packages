# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.2.r105.gc630d994f
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
f37064292f0eac2e97a3c6ee3c31131670de86e7 # scx_cake: 1.2.0 — clean-slate EEVDF-parity rewrite
b89ef8ff3a5d6149208ac51d58e15c1104ad8939 # scx_cake: identity-gated preempt fast paths + RT-owned CPU avoidance
4dcfe052d714d8e2789f192917bac333aa583c7b # scx_cake 1.2.0: K+L+M+S1d checkpoint — game gate passed, sealed benchmark wins
c3b0a3485d113e3b76634c1a15d6ccf7d374cafa # scheds: Introduce scx_maestro
c651d76939a3c4ad173a5f79e3e4281c41480ab5 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
0acf420563e2783a346c1e021b070515979ff301 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
5fd42d43c5e53fc75ed6bb93b519c3805d7c8119 # scx_maestro: Don't update deadline in case of re-enqueue
87ae0ffd2363d28cbbbc106210842bff63feb978 # scx_maestro: Introduce completely fair scheduling
5724fe7aa4cf52a44f7a23cba16cfdebcdf2f265 # scx_maestro: Make enqueue + CPU kick more reliable
a5adf0bd68580f04264e36cfc03b35db376d4b58 # scx_maestro: Enforce preemption each tick on CPU contention
d22fa27163e55b42c174a0b1ce29fb3709699cdb # scx_maestro: Temporarily drop sub-sched support
7be408ea40009bfd2db0118cf3dbf8ca5748ba90 # scx_maestro: bump to 1.1.1
6b22bd76a18bee800564ea0784045ad47681ec13 # scx_maestro: fix formatting
0ce236cce31b49cd2338401b2ff79b72e796dd09 # scx_maestro: bump to 1.1.2
ec28a8880ea4d22d4dbcf140beca994438c0026d # scx_maestro: Use scx_bpf_task_set_slice() setter
c5653727a49584bc7a51c79d2577e89fabdd6103 # scx_pandemonium: bump to 5.17.0
aa2f228eeffdeef8f9dcd51737c9faa89c3b96b9 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
