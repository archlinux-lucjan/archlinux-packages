# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.2.r122.gff86d6588
pkgrel=3
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
66742bb55e54122c2d67e8bfb6c74b40869fba10 # scx_cake: nightly squash — rebased onto upstream ff86d6588 (2026-08-18)
2cdcd647daf98124e7eb677ea38f387d5411a9bd # scheds: Introduce scx_maestro
56221cfe2f95a32bd4730c827d515f5617b8e27d # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
ab73f8cdf042f3b59d25a3d1a790fb4069b0376a # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
76f238c3327e262a1acde8d2eace92ffab210f82 # scx_maestro: Don't update deadline in case of re-enqueue
387b01e97e41da874047d4743a933d97543c77d7 # scx_maestro: Introduce completely fair scheduling
dd6a5a99081f9f99d839f82b5f54de570f186621 # scx_maestro: Make enqueue + CPU kick more reliable
52702a0ff54167087a351d3d1b7801a9e571de4b # scx_maestro: Enforce preemption each tick on CPU contention
43e6f90847dc9a73df76b9b7517a69bb734d0957 # scx_maestro: Temporarily drop sub-sched support
2f83e8a9629d2223fa3ed98bd3460582f52711f6 # scx_maestro: bump to 1.1.1
08a363a99c0be6ed4750e5ae59be3692bdc8845b # scx_maestro: fix formatting
5e917182720ef7fe8b27097702ff6da2cf7b298c # scx_maestro: bump to 1.1.2
683931a3845ed6a80aeea1d61ffa31e574816beb # scx_maestro: Use scx_bpf_task_set_slice() setter
ce075a5abf0c3b864a18a159a703d01b3d1436f2 # scx_pandemonium: bump to 5.17.0
17f55bab92d4f2e2358d09aa198090b40e6d34b1 # scx_pandemonium: bump to 5.17.1
942b48621bc582ca13a804bb01609a0729b20fff # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
