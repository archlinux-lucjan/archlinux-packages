# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.1.r201.g19ed8749
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  cmake
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
37555c22448e4ab6ca5bc133cefbddeaa1f8571a # scx_cake 1.2.0: hybrid-queue champion (LLC-vtime + hybrid4 game-tuned defaults)
211c69fa0d450191f9d61b414367194e699370b5 # scx_cake: fix watchdog stall at source — drop llc_pending bitmap (Option A), remove rescue bucket
7c3b1c9e9a7b0a3d3192da6dc03dcea96aa6e215 # scx_cake: silence build warnings + cargo fmt/clippy (no functional change)
b63b5eba0a3b2ccddab2ed51a8fbbe5a3ce61b7f # scx_cake: remove global core_steal_pending_mask bucket (per-CPU truth)
cec7505bea02428d59101bf0c12df63427b1f0f9 # scx_cake: silence debug-build -Wunused-function on config-gated helpers
781651d2e9efccc0c5ae09450df4c53522a05cea # scx_cake: promote vtime-local nightly candidate
4c220eede2da76f434b1f47f90f948c9c54c0e93 # scx_cake: unify local-waiter wake (preempt-kick) + free memcpy slice; gated service-readiness instrument
231f0daf796b073d8a1425ad7d355694366c966a # scx_cake: dedup shared-escape twins (unification, behavior-preserving)
e8b6e31e8c552a5bd89717f3466afe5303ccbec8 # scx_cake: EEVDF-like wake balancing for throughput class in vtime-local select_cpu
091a866b751ec1dc417fa6b4b7543bb4998111d0 # scx_cake: drop redundant self-kick on local non-preempt wake
92cda48bc6e2dfb56c463a40f339842999de2ddc # scx_cake: rebalance throughput class off congested prev (EEVDF wake_affine)
31d2a3b4e186d50a46a70c6257f335366c9843a4 # scx_cake: fix Cargo.lock
0e2139cfbffdc617b73121c3a759704275d5a999 # scheds: Introduce scx_maestro
23f705b2bed1dbf174960561539b2ad9bdaf9c5f # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
e3f0850cb0e69618cb88079052d92fee97483481 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
77a72c4393cd6743ea69231d6ea79edae159cd83 # scx_maestro: Don't update deadline in case of re-enqueue
353cf47b6cb4688df76f0c2119dac21a64ae72d9 # scx_maestro: Introduce completely fair scheduling
382be879b87cbc45b4af9f75c4607db17a1c28fb # scx_maestro: Make enqueue + CPU kick more reliable
2e3b4487b7aa7f02a607e102c6cdcf83d67d8800 # scx_maestro: Enforce preemption each tick on CPU contention
f41c50da40b13b317af49536b37861b38dd7dee7 # scx_maestro: Temporarily drop sub-sched support
8cff41c676f37f9ab3b067009a620fdf835fa5ad # scx_maestro: bump to 1.1.1
c5a6104c13a7727d2c8cd6886431daf682403631 # scx_maestro: fix formatting
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
