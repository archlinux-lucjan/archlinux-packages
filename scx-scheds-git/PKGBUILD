# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r342.g28ecba74
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
bbee38874eb827ab43c8fa04308a31f9725fb6ca # scx_cake: Sync Frontier optimizations onto latest upstream main
30d5d043da2f51646da3e785759137404704c50b # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
01d34210e6fb7048d8c572e2ccea4bc9546aef31 # Revert out-of-scope Cargo.toml change
70e7a6486382eb35105483631fefc90d43dd29c3 # bpf: scx_cake: Implement BPF-native clock cache
b8c7f280fef0d52a1749391303aa74db008ffb52 # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
73329ed20695c0e37f3713d05ebe34beba5245c1 # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
95b329fade266d341e9c7ba2358d6d99664c8ec9 # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
c904935c584cdc12a8d9c6e0088d7be3b9be3613 # scx_cake: ALPHADEV Phase 4 & Phase 9 Architectural Flattening
821d5dbbd73a16233df024a49f7930df46b93216 # Additive Fairness Model & Holistic Efficiency Overhaul
7dea28a7c43f2e9f01d0e657f55a481c8b1b330e # chore: replace memory init division with explicit bitshift
076bd975bf61fa7feab76f75f5c2ae5752675bb9 # chore: Update .gitignore for telemetry logs and streamline scheduler build
89540ebe45cc8512f67350eb1d2304dd1c251aa1 # chore: exclude .codex from tracking
c76ceb97b4a8bccd129bc99d770a6732e87d1bb4 # chore: exclude perf.data from tracking
9a1cd3e0fa2dbeb5745f18de8e939b28422d3268 # scx_pandemonium: update to v5.6.0
53efb5e0928d3de559c49e00a09b92882af3348c # scx_pandemonium: fix verifier bounds for older kernels, restore veristat baseline
7e4c39d01d5ff1aad805dfbebb19a309e183db3d # scx_pandemonium: fix interactive starvation rescue, verifier bounds
daa787a78f98bc8f9dae5c4b9e033a2c55beb9b9 # scheds/experimental: add scx_flow v2.2.0 scheduler
99b457da476aa90b8485092570f9fff6084e57a1 # fix: update Cargo.lock for scx_flow v2.2.0
4cf97fd6bf38d781250dad311a59a8fcfc566fae # scheds: Introduce scx_maestro
23fa9abe18c679a748c9e68a73930880975b0bb9 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
9a4ba063e3ac757c8fdda39dfda9e65ad0cc5539 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
57b67c83e5597883ddb562d7251805e9f94496be # scx_maestro: Don't update deadline in case of re-enqueue
b00d7b7596c5b7e41da9b667559ebf8fc11ab81f # scx_maestro: Introduce completely fair scheduling
c078ed285812b919c503bebffb9b62665a923514 # scx_maestro: Make enqueue + CPU kick more reliable
58489674cc4d8e7bf4befe05d36d8ad0ed34da79 # Cargo: fix formatting
31974980f3a2cb6b26650145a80ca047dcd49077 # scx_maestro: add --completions flag for shell completion generation
8baec8b05b7990e71b84bdefe3cd0cc67c24354a # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
