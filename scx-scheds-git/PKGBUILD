# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r305.g19d4b348
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
24e850f40e5efb400f50125c076f93f27fff7ea5 # scx_cake: Sync Frontier optimizations onto latest upstream main
980b2bbb95678da6c4a66aedcd792ffa44cddd4b # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
9a6de77c8d2456ef9842232477df64a8239daf0e # Revert out-of-scope Cargo.toml change
d86997666111b03d1e50a529dce9a15522f965f0 # bpf: scx_cake: Implement BPF-native clock cache
a051aa5724c2b0a1aa14d28bb0c2db6cb74b9dbd # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
f9d5f34088e9266a942fd9c3c59de8266378fc80 # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
2e919cb536f43c18b1bc62f5ed72358947768b2f # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
d7d59c165fdf859217485bdc8a858290e42cd3aa # Add scx_flow scheduler
29dd8ff43363b1e0c2a23b434f273a438b161826 # scx_flow: suppress generated vmlinux.h warning noise
ffbbbe50af896f107a5d587819ba645fa187d992 # scx_flow: report full build version
7d25856ab0663ab5a8baf7a220f641cd0d2e69f7 # scx_flow: use relative LICENSE symlink
8d23027851f798312ed29edf2cbc3e235cb16730 # scx_flow: simplify normal exit logging
dee15a4ff73b576bc6566121b1ab1bf2e870ad90 # scx_flow: add v2 cpu-stability bias groundwork
28f828d43b7bb5c1b5ce602d9e29284b3406406c # scx_flow: add v2 latency lane and bounded hog containment
7b868987411705c1b2e0cf72b8bbea1a06113d18 # scx_flow: tighten v2 hog containment thresholds
3284ef4ec4cd506cf6d57c4aed8c319b6372be87 # scx_flow: widen v2 latency-lane admission
07769c88f88710058cc8327ee8589becc3c6a0ed # scx_flow: add v2 latency routing diagnostics
607c62c7bc4829f68f979f3e0a06853f0058c2dd # scx_flow: keep contained hogs off idle local fast path
f4e448cd6439c0a5891617f15a072f6eb228d703 # scx_flow: freeze v2 lane baseline
0c4a34700b506a728ad1a10a736ad51673104d9c # scx_flow: checkpoint v2 repeat-validated baseline
2394f012a25cba13ac086c7df2b782b4fbe1c4b0 # scx_flow: polish v2 release and make policy state per-cpu
d6d58235354c124b986831447a730806ffb174bd # scx_flow: release v2.0.1 safety baseline
69cc159544697fbad73806034b7bd8f8dd4ef5bc # scx_flow: release v2.0.2 direct-local baseline
954ef7a6dd1fa58724721fdd304e5935abcc9375 # scx_flow: release v2.0.3 confidence cleanup and IPC continuity refinement
7370eac42d85d60b4b17520c2ea49f5ff6e0aeb0 # scx_flow: release v2.1.0 hot-path cleanup and live-gaming polish
a58711ab1e4fab44aaf8612ec81e9487f4d09a0b # Cargo: update dependencies to latest versions
815d24f5de68884f24a05c33c77013ba8143dfdb # scheds: Introduce scx_maestro
de58110ae07cad46c3d3117e490d15afcb69be86 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
58715960a91e0704d24ebb92ef7ddbbc9d42257a # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
ffee15eba9e18c6bf1df2045616e8abb26844ad1 # scx_maestro: Don't update deadline in case of re-enqueue
f3a448c95edf8454a95c5fd59f9374cf368f3ad6 # scx_maestro: Introduce completely fair scheduling
02dfde01d13bdea356c3a60eb1274b63c3f18a46 # scx_pandemonium: add PANDEMONIUM scheduler v5.6.0
af4f411117ab9f0b201e0cda741165855987195f # scx_pandemonium: fix verifier bounds for older kernels, restore veristat baseline
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
