# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r320.g14dda766
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
4a93f24414ddf8e96fd340769ec45555243cfd8a # scx_cake: Sync Frontier optimizations onto latest upstream main
7853bea1287f349427669239c74a3b061c2eff8c # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
a1f0a48b66cbcfb38f6c21af6632ad1954b977f6 # Revert out-of-scope Cargo.toml change
d4a13c6758614ecffb9483efe3c9898bfab0d2ef # bpf: scx_cake: Implement BPF-native clock cache
f4bef492b5107a0ef3ad03f8cca3f9e8a3bbe07c # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
7a6f871e3fc6e5d1bd1dfc77c04861731317ecf2 # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
d0ef8dac5b2b2338266342020f3cefef48eccf62 # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
ad6f8258de5d46f2a408d4499c01624e31a82ba5 # scx_pandemonium: update to v5.6.0
09dfe026c91af0459e286b57962f17340b95506d # scx_pandemonium: fix verifier bounds for older kernels, restore veristat baseline
81f2fc543155df09f601289ff12b90d803b9a64b # scheds/experimental: add scx_flow v2.2.0 scheduler
d170e3b4532855de047fc49ed08048602421c74a # scheds: Introduce scx_maestro
5567d94b02897a82b48bcca1c2377d59d18c5977 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
1944b89dbfedc6141f49bc2b0251600bb9c0f385 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
3221e76e9a65e808e9e8046cf18870e24a9ef5b6 # scx_maestro: Don't update deadline in case of re-enqueue
8ff40fbca303be837353ce8791eb1a4b3ce3f21f # scx_maestro: Introduce completely fair scheduling
b09c1f9091ee6b9ad734e5440855d98b61643595 # Cargo: update libbpf to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
