# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r258.g2ad3f585
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
c40b62c1d51eeb62a5c3f303dc3bab9852872f6c # scx_cake: Sync Frontier optimizations onto latest upstream main
c5a427078d614552aafb29faf71157ee23131811 # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
5f83f289a713436e5b15c258e448cb39cad8a7d0 # Revert out-of-scope Cargo.toml change
6c66ca92462ea8a77857f38b0ae0cd4c80daeb27 # bpf: scx_cake: Implement BPF-native clock cache
d0b9ce18f40a4f65c70448b0a38feb1a5bf46bf9 # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
259b66d3e422d95ee92ae06d44cdcdfc40131916 # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
a43cb739277acd2bd5b7471e3007ca8fe81e5b03 # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
a0b8a843296a02dd2388cb6a161ad13c520a526b # Cargo: update dependencies to latest versions
814485bc35d8c1977cc5aea6318fd0c3b38b8c74 # Cargo: update libbpf to latest versions
815d24f5de68884f24a05c33c77013ba8143dfdb # scheds: Introduce scx_maestro
de58110ae07cad46c3d3117e490d15afcb69be86 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
58715960a91e0704d24ebb92ef7ddbbc9d42257a # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
ffee15eba9e18c6bf1df2045616e8abb26844ad1 # scx_maestro: Don't update deadline in case of re-enqueue
f3a448c95edf8454a95c5fd59f9374cf368f3ad6 # scx_maestro: Introduce completely fair scheduling
74d949a022e743e1b7b12d5dfa6b5c9e6502cd99 # scx_pandemonium: add PANDEMONIUM scheduler v5.6.0
90f627861909cf3d0f91f725015a8fd944311ed4 # scx_pandemonium: add veristat baseline, use scx_utils build_id
d751cf346897befb92a8b7853c072cd2c55703be # scx_pandemonium: add veristat baseline, scx_utils build_id, ops version suffix
4e8d8b1b329ad88314515aba4b4f63a00de98973 # Add scx_flow scheduler
24dbb30035e7cb328822131991bc2d9b23599942 # scx_flow: suppress generated vmlinux.h warning noise
16f900dd9e593206783c70023acf10454d29041c # scx_flow: report full build version
fb216d8c5404c28680acf30f941a8666a0558bb2 # scx_flow: use relative LICENSE symlink
c7f3c23df5945893a8ff9cc477875b5eaecd5cf7 # scx_flow: simplify normal exit logging
657f8e04979209181c15708226efa2afb3e38610 # scx_flash: Re-introduce SMT contention avoidance
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
