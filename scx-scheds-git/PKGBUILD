# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r42.g8e265d62a
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
0d79be3d55470027780eacab764c2ffcf11e0cac # scheds: Introduce scx_maestro
6bedd985db80ebc6c66599dbb5dffb9c50e4c42f # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
ecc92893bddb1a55613914c020e879057b58e361 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
96fe3d64cbecb40fde58ad84b7f70fdf5b409c9a # scx_maestro: Don't update deadline in case of re-enqueue
4d1695703bbd23cf09ade63258b45218fba8c83c # scx_maestro: Introduce completely fair scheduling
c0cf5244d76b037ed8207c3c4d981789769402b7 # scx_maestro: Make enqueue + CPU kick more reliable
0ed9843f7c79fc02baf2c807115b2e4db050ad66 # scx_maestro: Enforce preemption each tick on CPU contention
6f8a1ae7a252f9b6111ba6f0738b384dc72250d2 # scx_maestro: Temporarily drop sub-sched support
54ac33ffb6bad916a0c975eec9566981a67bbee6 # scx_maestro: bump to 1.1.1
2a4b087cea57cc34703895d9d409d44a301b867b # scx_maestro: fix formatting
27ecc931ee6791e0a0f52a321575260668efbe26 # scx_maestro: bump to 1.1.2
d514058ab04d08f1b2dee9e3d87e32f0c12e990d # scx_maestro: Use scx_bpf_task_set_slice() setter
3245bfbda51b43c432989a8960fce9bd6525bc79 # scx_maestro: bump to 1.1.3
c921d827ce77c7e7bf2f76d29cc72a8435f5352c # scx_mlfq: bump to 1.3.5
feb7409088898c0f23114274ff433fdff89a101d # scx_mlfq: bump to 1.3.10
5843fbe0b3494d6d8187d038272678a76c3c3fd9 # scx_mlfq: bump to 1.3.11
c8bf8fa83d9d64c935c8eadfbddf4ae9ec9ef5c0 # Cargo: update dependencies to latest versions
cbca8fe111660440c13ad14bdae09ea4e6d710f4 # Cargo: update libbpf to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
