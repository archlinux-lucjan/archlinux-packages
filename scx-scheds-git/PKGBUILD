# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r189.gbe055c5a
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
6896c59086a172d0e5c6a1c8a87d299ae4f4801e # feat(scaling): compile-time hardware detection and gate system
5ce56ca2e4593f86ab511dac4f97b104c728a7d8 # fix: sentinel collision at 256 CPUs, gate2 fallthrough, libbpf logging
960866df1b1fcdd420ea48259df4911caf109419 # cake/bpf: assembly-guided instruction reduction (-35 BPF insns, -3.6%)
e1c8d78bdf9498228b4ee2b816d6a6161db8f298 # cake/bpf: frontier hot-path optimization (-9 BPF insns unconditional core)
772c7f4d1b869b29b538454f2d32dbca048e9bfa # scx_cake: comprehensive comment cleanup + README coverage expansion
fc9d469433d7ff18618f7149c5d1894ae52f6656 # testing performance classification changes
40bc49ade0176798c55610aaef51fe3f65b9bd44 # bpf: execution path flattening — hot path 504→409 insns (−18.8%)
ecda3bfafa2db9dbc615d7e464c5f7876032c61d # scx_cake: Implement Phase 10-13 Hot Path Vectorization Sweeps
0575ea37f419a13755c48c47af84df08dbdde5d4 # scx_cake: Implement ALPHADEV Phase 14 O(1) Kfunc Kick Bypass
1df634b267d70de9f8869f2b3221d61a9d2c22e4 # feat: game detection ACF manifest overhaul, stable-logging, and origin-aware kick guard
6c8097458e78c23f979d1139a6c8c8b90b3ba653 # fix: stage missing bpf_compat.h header for atomic macros
50d7254f42a6c7ce2741c41c15107ac87d57e28c # fix: remove child_count threshold for Steam games to enable native linux multithreaded detection
9ff900fd0aa7cb721cdafdb9c47810fb04b22a50 # scx_cake: Frontier Optimization Phases 1-3 (SIMD Offloading, Branchless, Oracle)
e8236e6c70d69f1ce0bf587871ada79a0321f853 # scx_cake: Frontier Phase 8 & 9 - Asymmetric Vectorization & Branchless BPF
c0711e77bdcddf3211d6fc9b3abaada791e65f23 # scx_cake: Rust Formatting & TUI Dead-State Sync
08c01ec22359072ef6b0d6ec536e85bc562292ff # Cargo: update dependencies to latest versions
08f09e33e52c6b051bc338f77e9ef13ebb018c31 # Cargo: update libbpf to latest versions
1886c2e173edeba6b9790cdb540f1c49e6fcbcb4 # scx_utils: Handle ops.sub_cgroup_id in older kernels
51619164aef77def3ebf69d018fe1180f27ab66e # scheds: Introduce scx_maestro
e9cc3a7017c14dc3adcf2d98687f8d2ffaadcf11 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
96ac97eef3b31d2c66b7815265cb13223f8263f1 # scx_maestro: Add better NUMA support
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
