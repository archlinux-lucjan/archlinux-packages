# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r430.g81738161a
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
cb3825b74f095947e779de7015806f8101cad321 # scheds: Introduce scx_maestro
89246d26e5df3afe7d856f0945305e95319b9a62 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
9b60fbf593d0a44cadd98f6a8a91aa5e54c29500 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
c42b7f8594f3afb642079b92f8ff5b9c81e8470d # scx_maestro: Don't update deadline in case of re-enqueue
fcf0d456722654e376f71340d6988608f8b2cdfd # scx_maestro: Introduce completely fair scheduling
34e784078b453f7ec9ee5606e89e0656cb701233 # scx_maestro: Make enqueue + CPU kick more reliable
efcb92fa3bf51b97f69895108d0f238d03cc35f6 # scx_maestro: Enforce preemption each tick on CPU contention
943bedc096c2faf118948a44ae3de9d229b4cce8 # scx_maestro: Temporarily drop sub-sched support
a46f165036d3c444cfec65e77f7187316845f6ea # scx_maestro: bump to 1.1.1
00959a30ecc6d82a7444dde0dc075c04b607aeb1 # scx_maestro: fix formatting
3945004122a603a26539c8ef0a343d752ad5962f # scx_maestro: bump to 1.1.2
0214a5ff7f99c9979d15a74381eec0eaab979bcc # scx_maestro: Use scx_bpf_task_set_slice() setter
86ac24a79c209749c4c0f37e1aca2e0804ac7cb3 # scx_maestro: bump to 1.1.3
4ced5de58a13c6b5ceb1a59dbbe66b167e33a1fc # scx_maestro: bump libbpf to 0.27.0
d7cb6b6e7be707018919ff766e4a60368d4acdc6 # scx_maestro: migrate to Rust edition 2024 and rust-version 1.91
0796632ff7dac45b14c7a9b90fde3517d0057201 # scx_maestro: fix formatting
26eca9a7b857170c8cf8afe5398cc62b9548f5b5 # scx_maestro: bump libbpf to 0.27.1
12f39e3cce7e29e7b9a5eb8b5c5e3a8e8a69893a # scx_pandemonium: bump to 5.21.0
542b9b04cccc975723c48bc77710771c94f1310e # scx_pandemonium: bump to 5.22.0
ba2a4e4abe913aab347b1cab7956b4fe58047105 # scx_flow: bump to 4.2.41-o1
3865f4fd2a8ab00916c7ce74072ed5bc7f23fa0d # scx_flow: bump to 4.2.46-o1
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
