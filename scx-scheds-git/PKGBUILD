# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r84.g0d1df6a05
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
a9e9094887831b64d24cf164915837a2f6e041e1 # scheds: Introduce scx_maestro
52f41d3dea55c1a1d20475acedeb6f0a88b8fd9f # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
61c4169480d00e9526c55ae16738e2322682fd78 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
9c44f49da897daa9cd95ec97925c425a1c5ca9df # scx_maestro: Don't update deadline in case of re-enqueue
70bbc532bc6ed73c69e31c7fcfd721ad295f04bc # scx_maestro: Introduce completely fair scheduling
6ba4a660760d4729c0d2e8b679b146de3464a30d # scx_maestro: Make enqueue + CPU kick more reliable
4cf51c163604a46b37d5f8286d51e9ce6778b19a # scx_maestro: Enforce preemption each tick on CPU contention
202f7e0242c996b33df8ab77da517d5ca2c28616 # scx_maestro: Temporarily drop sub-sched support
58424cbbe471227a7c480199d7ed20d66ac729b3 # scx_maestro: bump to 1.1.1
a59909ae06f564e0271b46b25ee3644bb98bdf5e # scx_maestro: fix formatting
20cbffc41202b75f42363fccff3d0f825fbcdaa9 # scx_maestro: bump to 1.1.2
bff867c925f6735b13b6e05439d6d14efe97191f # scx_maestro: Use scx_bpf_task_set_slice() setter
cac35e2d16a95cb50197820b5892db977c8b21e0 # scx_maestro: bump to 1.1.3
fcb251953e2eda851f6106c78e6f0781c26920e0 # scx_mlfq: bump to 1.3.5
0422398f73a325dd9550d2daa44f0821fb1281f0 # scx_mlfq: bump to 1.3.10
8d44fc8d8f33c65857251ae52c13184dc23c4b1f # scx_mlfq: bump to 1.3.11
b55fa10e18eb080089405d82fc73e63b570effc1 # Cargo: update dependencies to latest versions
1c521abe50ae80b0090b8de38bc08d0a95789d2e # Cargo: update libbpf to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
