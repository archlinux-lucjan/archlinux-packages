# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r426.g3b7a3bf3a
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
7138e24e410b31f21e560af0dd9112b3bf6a2332 # scheds: Introduce scx_maestro
e93adb2a27bc825ae7a81c7538ba0dc36f345c67 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
0cfba83ccc020ba57b2576abdcb2ad0987f613c8 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
ba1d6b7d8c0bfb07217bb5572cac832e50ead5e6 # scx_maestro: Don't update deadline in case of re-enqueue
7397f354f9a76d776446275beff4f1a15e43bb4d # scx_maestro: Introduce completely fair scheduling
0764d6687de764fe52f49979ce1c511b722707df # scx_maestro: Make enqueue + CPU kick more reliable
594196a4d11389c08d9080da21714ae71ee3a0cb # scx_maestro: Enforce preemption each tick on CPU contention
67c51e88531a96cd359b42b2604125d565d15425 # scx_maestro: Temporarily drop sub-sched support
40d42eca1682875f724bc6a80076d45b0498b380 # scx_maestro: bump to 1.1.1
9d65cf98df8a0726e9b5d768f8cb4c6444b4466e # scx_maestro: fix formatting
15c9351ce3c369c7d4b0c38b6e46d3f69e9e6ab9 # scx_maestro: bump to 1.1.2
e8aeae52683ef9a39fbd20e6129f7b07199363d1 # scx_maestro: Use scx_bpf_task_set_slice() setter
139b437b371b03effb1c4debe28d56d947dfca0d # scx_maestro: bump to 1.1.3
3026835bda30e6afc1b30cdaa7de678f0be92d53 # scx_maestro: bump libbpf to 0.27.0
ca75077669d365c6453592f0ff0e4fa6fe375abb # scx_maestro: migrate to Rust edition 2024 and rust-version 1.91
9c15bc731c253fdc628053a84091c03b1affe45d # scx_maestro: fix formatting
f355a6830b6119e12084b851d06b3a2fa2f4d3e5 # scx_maestro: bump libbpf to 0.27.1
a7791c2fca55ff27cdbedcfe58a8ee65a6698e77 # scx_pandemonium: bump to 5.21.0
16fb40fcf3e23434c0f68dd7f502764c77eb1c34 # scx_pandemonium: bump to 5.22.0
92a70f7abb21440bf5bd71909372c96267a75ac3 # scx_flow: bump to 4.2.41-o1
cdc4069fcaba43b9e2be5ab80bb631ac43428c27 # scx_flow: bump to 4.2.46-o1
e00b26131e5674e14a46c23deb442aa28712591c # scx_mavd: Fork lavd for an arena-native cid conversion
2fa72f17a8f6666b0c96af90bf7d85728f371d4f # scx_mavd: Move scheduler state into the arena
0efd0c7fec7f747fb45674aa896cc2cb9dafb056 # scx_mavd: Convert to the cid form with arena CPU masks and contexts
e90189e8b78fd0fa000ce4b29acab7ee4f531e13 # scx_mavd: Simplify arena scans and reuse validated CPU contexts
336365ddbaf182800a6dd93fa0e9019f03be3533 # scx_mavd: Name cid-valued fields and cmask globals for what they hold
a0d129545e9b1a8dbb4ff718eb4120aae854a35b # scx_mavd: Document the verifier limits behind the noinline boundaries
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
