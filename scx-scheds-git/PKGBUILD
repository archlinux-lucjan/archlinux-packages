# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r246.g677469efa
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
988e9346c65197920366ab4fff1dae31483d816c # scheds: Introduce scx_maestro
4eb64ff5e8adafebc31da2f64946545fab84861a # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
495b4b5c4fe19e2de544a1361dc74c5ad0c4901f # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
cf86b8f5d066234c4e565d0906bd0d1db801ac6e # scx_maestro: Don't update deadline in case of re-enqueue
800870b8bfcee5eeffaaf2dcb774834ca7a7794e # scx_maestro: Introduce completely fair scheduling
9cf9dc40e6a6e6d228fc457fc6474c268a4fa12e # scx_maestro: Make enqueue + CPU kick more reliable
e6e5adbe82817fd6deb863a5fd144687ada76e05 # scx_maestro: Enforce preemption each tick on CPU contention
38085e21de3fb5a07e78d1a30451b715d7221642 # scx_maestro: Temporarily drop sub-sched support
59a1be84ad9b4b0c129b788308bd883ec649aaba # scx_maestro: bump to 1.1.1
9fdf59b97c54c567c28c482e6ebe35ab752140fc # scx_maestro: fix formatting
4fa65727e32b9467ed3a20b71fb2e6e3612784e2 # scx_maestro: bump to 1.1.2
dc83e5d757f9c88a56fa07e192d67817538bcf04 # scx_maestro: Use scx_bpf_task_set_slice() setter
5d02b95eec6d1463f945c31337a9728be290ee0f # scx_maestro: bump to 1.1.3
b5f1eda8bc4f5d685f951657ff28f50d4be38fe3 # scx_maestro: bump libbpf to 0.27.0
36e11fdfc882979800ac0deba5a8a6a3aab90c1b # scx_maestro: migrate to Rust edition 2024 and rust-version 1.91
9409def2789a081fd5433f981ae8bb3d51a4a889 # scx_maestro: fix formatting
3bf28bd676e7c54ab11f1bf54892d1c30cf376d0 # scx_pandemonium: bump to 5.21.0
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
