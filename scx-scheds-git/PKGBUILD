# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r104.gd6a90c82e
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
0e609413d8f6dc3ecc88d2b7be072e347acdffb2 # scheds: Introduce scx_maestro
05f55beee9bd4e73d5f22bbf24ab8e0ee03781e1 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
b9d527f6086fb525d1ee42dd2f691467ac556da0 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
17b8e5de89e9b1fd4e26d3950692b2e9fbd4b96c # scx_maestro: Don't update deadline in case of re-enqueue
47b0569dddc2a51fcdb1cbf9be0bb3a218be763a # scx_maestro: Introduce completely fair scheduling
08367691b98ff94e24b9478dc4409e6d668530c1 # scx_maestro: Make enqueue + CPU kick more reliable
f827f64ce8e62702f0c657d682ebac6bd0d52f8c # scx_maestro: Enforce preemption each tick on CPU contention
17c48d903bae22e804ea90d95181bf5b5469acc3 # scx_maestro: Temporarily drop sub-sched support
e378d0c62b146028b97a7af31caa158c17b82391 # scx_maestro: bump to 1.1.1
be4f47f3bb630a249de0261ffddc3edb912430d5 # scx_maestro: fix formatting
30ae06da3dec861831e79fb04e0c81b4f5afe1e9 # scx_maestro: bump to 1.1.2
967137bfbda39d2b9a22e8829c56e3217ef333ba # scx_maestro: Use scx_bpf_task_set_slice() setter
a26c8704147fc7af55d3eb1155df0f35e364202f # scx_maestro: bump to 1.1.3
968b25b43c46f384eadfdd70c27fbebbf328f4fd # scx_maestro: bump libbpf to 0.27.0
71dac544317caf53901a69126201ce777e9d692d # scx_mlfq: sync to standalone 1.3.11-zero (zero-alloc + Typical Use Cases)
11fc9755247d36cd29a7b20f4636633feb8c2e8b # scx_mlfq: fix veristat on 6.13 for dispatch
302bf6bd4f55628745a8c1152aa46543cc18556c # ci: disable veristat for scx_mlfq on 6.13.y
9fb7e8de3bacde57ca023fd6e5390d116b6de519 # scx_arena: Separate userspace services from arena setup
2c4ec9c71a839e457950349ef7ffb7f01da3f188 # scheds/include: Introduce compat cid kfuncs
0c61fcf13640e09d9288aa71d3fcb576704a718c # scheds/include: Add arena-backed cid bitmap helpers
001c5525b6378ae4ac93a25a7dbd0fcb41453336 # scheds/experimental: Introduce scx_cidland
f2a195a2d93b95d22bb2aac6067a89a0ac6fd645 # scx_cidland: Move per-cid topology table to arena
75bcdfa70bac77421bdc7cc731c80af3d3f50245 # scx_cidland: Link the arena task context allocator
0e61bb28271d5af7833cd84a4832196f27b35da4 # scx_cidland: Move the cid bitmaps and task contexts to arena
786cb1155bb3f23e4ee2613769b49fffeaa022d7 # scx_cidland: Size the cid keyed state at run time and drop MAX_CIDS
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
