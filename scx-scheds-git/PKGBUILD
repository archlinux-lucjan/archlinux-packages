# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r340.g36cadac5
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
f5b14d8dc773f698fc2565ab5e73b293d99ca4a9 # scx_cake: Sync Frontier optimizations onto latest upstream main
3b0c7df89e299b141a6f0d49012c2d20a33d9e44 # Refactor: ALPHADEV Performance Audit and EEVDF Correctness
fa105bc482a643d3b70802da9814dd798275a420 # Revert out-of-scope Cargo.toml change
cdcd97ad5af3c9fa77a952c2f08b5a9c9b0db2fc # bpf: scx_cake: Implement BPF-native clock cache
0fd9eb090f9abffe2135cff8f7719f41cb8f49d5 # ALPHADEV Phase 16: EEVDF vtime correctness & kthread domain alignment
9aa458d62f04c3d314606e1dab85a2b00342235a # refactor(scx_cake): mechanical sympathy overhaul for Multi-CCD false sharing and latency
f8ac5261ae1f61fc5c908214f05824b53b8e028a # scx_cake: Iron Curtain tier isolation + sleeper floor relocation
9c85c6efca0fedb8a4fbf898a698ba4424c0aa74 # scx_cake: ALPHADEV Phase 4 & Phase 9 Architectural Flattening
d511afe225cd57ee10e3cc8adb08303f1d52d32c # scx_pandemonium: update to v5.6.0
cb7d0b416f7db34a64fb7cdc9fcbb99224410fa0 # scx_pandemonium: fix verifier bounds for older kernels, restore veristat baseline
b0ddb104ee6da878f2d9dabe7b9bf73c89773ef1 # scx_pandemonium: fix interactive starvation rescue, verifier bounds
c9d1145060aecfd507167a0d0f4b022c40f300f8 # scheds/experimental: add scx_flow v2.2.0 scheduler
0a34b94df3c81310cd0dbb78eb9f6a6c7aee238b # scheds: Introduce scx_maestro
4e97efc45471d0ef5f58e8179d4699e7836ca682 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
616f8ab3dc30497759e48cf05584b8dcbf438ead # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
c8e60552ac53f19c6396c6503377e7dc1781f850 # scx_maestro: Don't update deadline in case of re-enqueue
ad928a714ba86582d96a1d19935a005560bc7c20 # scx_maestro: Introduce completely fair scheduling
2d79bb0f9746410ddbc1bc372d4d8250153b3488 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
