# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r248.g6c54f4f66
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
a41e339299b2bbdd932c100192cecf0f15913e84 # scheds: Introduce scx_maestro
e35e86d5ad469608fc1892f9df22a4235981055c # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
a770e7fd3d3deeaeb2502e09f51645cb645d461b # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
cdf99e39144ce0cd80e16899b1ab1539ff2ea543 # scx_maestro: Don't update deadline in case of re-enqueue
019db3e02cfabf7a5bceeb64f9322604501a780e # scx_maestro: Introduce completely fair scheduling
028680b8b61ec9710a7f8046197e2476632cf3be # scx_maestro: Make enqueue + CPU kick more reliable
18b2e5d54bafc52572ded8e575ca2002deb4be1a # scx_maestro: Enforce preemption each tick on CPU contention
0570a2fa208b16f54a0dabde198a1c79d95bbee9 # scx_maestro: Temporarily drop sub-sched support
da576e2e5927bcf6005f6c6d9b68e0584920ab58 # scx_maestro: bump to 1.1.1
cafe158c04daabd9f818094c63c41d7bc82635d5 # scx_maestro: fix formatting
d5f50586f231be6444f073451b35d238481183f9 # scx_maestro: bump to 1.1.2
8a22aca21afc8d623912309f9fdc94722b697f44 # scx_maestro: Use scx_bpf_task_set_slice() setter
951ad7624c2fad3d5d19cac39a4f97478bf682f0 # scx_maestro: bump to 1.1.3
6f89bdac7c105dc5d6e7aad620246b74733061b9 # scx_maestro: bump libbpf to 0.27.0
3bb68a407bb0a08024bf23bd425f847f6a5360af # scx_maestro: migrate to Rust edition 2024 and rust-version 1.91
40c43a76158ec3896d30f0688368b0fa9bb216e0 # scx_maestro: fix formatting
d50fab1a0ae548ced050ac5e043d7660b26a5091 # scx_cake: consolidate nightly scheduler updates for Rust 2024
bd249f0149c621862b5874e10870b671cce4465a # scx_pandemonium: bump to 5.21.0
65d6fe873f943d6956ad1008aceff954d835c6e5 # scx_pandemonium: bump to 5.22.0
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
