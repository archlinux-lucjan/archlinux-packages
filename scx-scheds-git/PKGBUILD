# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.2.r72.g964a5aa3d
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
7a6bf4ad69bb01a34da34368ee64ffddc558e626 # scx_cake: 1.2.0 — clean-slate EEVDF-parity rewrite
0a3f7a41955df095a00a9cfd6b157b73c28aa231 # scx_cake: identity-gated preempt fast paths + RT-owned CPU avoidance
e27451d7a22b9f1510ed74959d8cd6d267783c70 # scx_cake 1.2.0: K+L+M+S1d checkpoint — game gate passed, sealed benchmark wins
097df162d6029daae3d650072803e9b38f052584 # scheds: Introduce scx_maestro
fbdab53e04eec252e796aad0c8b21d4d69f04453 # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
d8c3db3e67723520ffa8bda0fa83eae175d489d8 # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
ee49d5182cf1641f32c9969a6b93ea6d87321b13 # scx_maestro: Don't update deadline in case of re-enqueue
9f143f0c24d7522d4f3c0bc19e3175e38f7747b1 # scx_maestro: Introduce completely fair scheduling
f5a18d0bb92e7a13ff7192cd349249aaf29cfca9 # scx_maestro: Make enqueue + CPU kick more reliable
d7781ad0af53fba49b26230aed5f1ad0e25f6462 # scx_maestro: Enforce preemption each tick on CPU contention
23f7d4016587f07c034a1bdcb4b29e4358a0df63 # scx_maestro: Temporarily drop sub-sched support
a68f780d2547269d159525674728fb7c0a67185d # scx_maestro: bump to 1.1.1
8e8eeac7e847c0abdbf702169c48816392b9b89e # scx_maestro: fix formatting
6ab6c29f900256799d6c40840c91e1b5e9b500e1 # scx_maestro: bump to 1.1.2
7a49d2cfb6f91ce1af4854bd1b66107298d52c45 # scx_maestro: Use scx_bpf_task_set_slice() setter
a4d990521388957e484dae8280f6420184b08553 # scx_pandemonium: bump to 5.17.0
cb9572d60cf46d8cc5699665ce283444fab2afff # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
