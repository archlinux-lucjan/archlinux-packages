# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r354.g01acee935
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
5a9141b9b14e4111537dae1743b095b260d12e41 # scheds: Introduce scx_maestro
d829c055b579ee58574efbabb648616d4f31f57d # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
8a007759b8c191e32896d130910c1a413ea51d7a # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
f15b8c1fa6e58861b662dc290855c989cc0b11f3 # scx_maestro: Don't update deadline in case of re-enqueue
af26f4e70dfd4b2fc17af8865658723d70d76af7 # scx_maestro: Introduce completely fair scheduling
9626465eff6cd7aeca0c9cf13d23bf9b3d87c7cc # scx_maestro: Make enqueue + CPU kick more reliable
2a718316090b07cc183d08fe98e764d0c8c05703 # scx_maestro: Enforce preemption each tick on CPU contention
b35d8b76ec73079efd5f940b86ac82b31b09ea8c # scx_maestro: Temporarily drop sub-sched support
d7a7bc05f3598b42174420413cc4ec34e7ee4219 # scx_maestro: bump to 1.1.1
80f50834b2eefa75027eb5e2293148c8f7244a1c # scx_maestro: fix formatting
1ffe26ebcfa30f861a6ea6a9259a67010228fcff # scx_maestro: bump to 1.1.2
17d96b07f5de980911a22a5f2511e104b643c682 # scx_maestro: Use scx_bpf_task_set_slice() setter
bba4f7492657d4bd561cfa59edcc1b0ba2e74d20 # scx_maestro: bump to 1.1.3
fb4449c476746450c8af64fac260747dae20f084 # scx_maestro: bump libbpf to 0.27.0
a9dbb8c20a38f1d4b36abe503622d1ea22c033f0 # scx_maestro: migrate to Rust edition 2024 and rust-version 1.91
e5e8c69ba0b5fe797931668d8443c35fe5bb9ce8 # scx_maestro: fix formatting
e699e63bdc0bb0188d164b533031b744fc67e791 # scx_maestro: bump libbpf to 0.27.1
5b747144459a33edf5243934a977fc140fd7fa8d # scx_cake: consolidate nightly scheduler updates for Rust 2024
3734f4f0230589d571320aa972c5e11bf11d4c8a # scx_pandemonium: bump to 5.21.0
6c8ac46a12663ac25191384df45357f1b3c66262 # scx_pandemonium: bump to 5.22.0
c83c1230c24704a5521ecbea861bd317fdfea79f # scx_flow: bump to 4.2.41-o1
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
