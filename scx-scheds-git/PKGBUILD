# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.2.r77.g2d4690982
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
d4306b203cf77acee215e5bfab23e052dd9e63e0 # scx_cake: 1.2.0 — clean-slate EEVDF-parity rewrite
122af4e7f38adf1ddeedfc361a1ca119f9202706 # scx_cake: identity-gated preempt fast paths + RT-owned CPU avoidance
b918d5bd2c7ac3e1dc680ba60ec2df1aa6828648 # scx_cake 1.2.0: K+L+M+S1d checkpoint — game gate passed, sealed benchmark wins
c6bc50ad47334cd6fcb3ed6ffac79415a611c8cf # scheds: Introduce scx_maestro
4574d9411a4b0e4a095f13f93a063d50557d160b # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
e9b337ab3c8d8d2fc71f9acb7a8124fb6919ae9c # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
dc9fe5658123023825a867df8d267bc2c8094a8c # scx_maestro: Don't update deadline in case of re-enqueue
2dffc18fdd871f888adf4d0b975c76d6f5076c23 # scx_maestro: Introduce completely fair scheduling
34e552a3a18270b249bdc1cf353d7bd1ecb19502 # scx_maestro: Make enqueue + CPU kick more reliable
b7136d9957b33c1b5a653ac23f4cb61c35a3d710 # scx_maestro: Enforce preemption each tick on CPU contention
ebd7621422bad5de89b442e303ecab6c83bbbf25 # scx_maestro: Temporarily drop sub-sched support
3a56afc363640654d22c0a47245ff4531bd987ad # scx_maestro: bump to 1.1.1
f11147e4a7e615b03c923fe13cc3d111431d574c # scx_maestro: fix formatting
45fb1aebd27a7ace46978bd14586f0034a749377 # scx_maestro: bump to 1.1.2
62813cc5ddf2a53eae53f3014d8046ff05cfb013 # scx_maestro: Use scx_bpf_task_set_slice() setter
869aa85f30dff5507727082a9e918764b2d6fcd8 # scx_pandemonium: bump to 5.17.0
01fe23a3cb6524d52fad208cbf7963baf3283e92 # Cargo: update dependencies to latest versions
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
