# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.3.r248.g6c54f4f66
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
68b963de03cd594e7b1f4489d946862e1879e5b0 # scheds: Introduce scx_maestro
08645c6fae57acdfa64c02e9ebc62569b9d887ce # scx_maestro: Honor cgroup cpu.weight for tasks and sub-schedulers
4093a5c09966bb9b56d5235ec4144114d3ac300c # scx_maestro: Allow to enable/disable NUMA and SMT optimizations
9e3c402c69a5a8abf460382d012dd6565c836afa # scx_maestro: Don't update deadline in case of re-enqueue
835b4f0b9950ea327d10a7448e0ca82952f46abc # scx_maestro: Introduce completely fair scheduling
df2b7cf3c78966a5304e73cd5b39f7af39b70ce2 # scx_maestro: Make enqueue + CPU kick more reliable
6962f03496dc7426770c8912e104e19e88d9dbb9 # scx_maestro: Enforce preemption each tick on CPU contention
e8f212095cb7c3f8588264571455f42db9cac47f # scx_maestro: Temporarily drop sub-sched support
1650edc3f7676dbf8738b5f25c0469978c3af0de # scx_maestro: bump to 1.1.1
00a9bb3299ee6b6b75a80afda2e0b4a429cdbabc # scx_maestro: fix formatting
76fca3fcd86ef30fa0814ed515a4e78ee4bbbdad # scx_maestro: bump to 1.1.2
374fe4664b0ccae55ae53a3036f8e1502aeca50e # scx_maestro: Use scx_bpf_task_set_slice() setter
ea239ecd2111b4d12118b05673084679a76a2d67 # scx_maestro: bump to 1.1.3
7a9e88eba2335bf87384025283c8ef0f40a371d1 # scx_maestro: bump libbpf to 0.27.0
5e75349e56d8e47f6005bcd342209bc779af437d # scx_maestro: migrate to Rust edition 2024 and rust-version 1.91
8e4f7cb2386fe4b9d256424572e07584ff90b135 # scx_maestro: fix formatting
9b95dc5b6a49e795385eb192bf25405dacacd3e3 # scx_pandemonium: bump to 5.21.0
86bf1dfc5fcad0b80480370ada7853531c2775cd # scx_pandemonium: bump to 5.22.0
d6526e7c06933230e20e8ccf8a673ab85ea582ef # scx_cake: consolidate nightly scheduler updates for Rust 2024
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_characterize \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
