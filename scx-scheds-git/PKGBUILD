# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>
# Contributor: Tejun Heo <tj@kernel.org>

# Available profiles: “release”, “release-tiny”, “release-fast“
# See: https://github.com/sched-ext/scx/blob/main/Cargo.toml
_mode=release

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.1.0.r171.g9d115659
pkgrel=6
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64')
license=('GPL-2.0-only')
depends=(
  jq
  libseccomp
  libelf
  zlib
)
makedepends=(
  cargo
  clang
  git
  llvm
  llvm-libs
  python
)
optdepends=(
'scx-tools: scx_loader and scxctl - A DBUS Interface for Managing sched_ext Schedulers '
)
options=(!lto)
provides=("scx-scheds")
conflicts=("scx-scheds")
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')

_backports=(
18767b2e97aaa730f20f26db7a8dac6556ca1f9b # feat(scaling): compile-time hardware detection and gate system
007cf967a75488f09de02f5827aa817f07c0a8e1 # fix: sentinel collision at 256 CPUs, gate2 fallthrough, libbpf logging
d2c090f50c2c87d11c77afedcb7ea8f4c6b66ecc # cake/bpf: assembly-guided instruction reduction (-35 BPF insns, -3.6%)
1c1405712d436ac76279a8fc1f358e86f90160c4 # cake/bpf: frontier hot-path optimization (-9 BPF insns unconditional core)
e48f95345a934bea1d018cf09cd682c329494144 # scx_cake: comprehensive comment cleanup + README coverage expansion
74847cef00cd203084fa16bbf9be4e237684e14d # testing performance classification changes
5b77b396a92e5e57dd6b9f2ec23b464fb8bc61a8 # bpf: execution path flattening — hot path 504→409 insns (−18.8%)
283d93117387cfe9a7e45476e3de2b8d0c5b17c5 # cargo: fix formatting
301ee3cdb84bc403657f1015d32707634864d118 # Cargo: update dependencies to latest versions
55b3ef0364eff2c8c5b103c1e08c029aa24b74e6 # Cargo: update libbpf to latest versions
2192b29d19feee712392f3e7662a8438aa660d2f # sched: add scx_cognis scheduler
0c56aab9b6153cb20e397c856d791e553029fcf4 # docs: refine scx_cognis README
99219b91a6b39d2732118d639d39ba914e7cd6c2 # sched: address scx_cognis review feedback
b53059f4c3f993d948716ce2f0ea3e832e19d564 # sched: log scx_cognis startup details
e8694b8a93a22880ed8baaa27a241f75e5a65e1a # sched: sync scx_cognis version to 2.0.7
a0b873f0b384bf6fe92d8916f265af02d7b0dd5c # sched: update scx_cognis lockfile version
e1def90b1c59365fe80e2aeec999cf32e16b468f # docs: link upstream README note to standalone repo
39f5167f6e5559595f35f8ed417da8541cfe18ef # docs: tighten cognis references
f32e82d84161a48b6d4034ce44971c28b9a519a7 # cleanup: satisfy clippy and simplify references
3e073c317cd794989f73a0910982870cae08ed62 # sched: sync scx_cognis to v2.0.8
74823f99d650b8009e80acd5e012982fd66a491a # sched: verify scx_cognis detach before logging
9e39307fa049d06c52aa7e88e5f87611b7f1b104 # sched: fail open on sched_ext watchdog exits
1c6e04ae910e5ebdf90c32ccff8e90cd73637517 # sched: quiet idle userspace path
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --profile=$_mode \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/$_mode \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
